# 路线图（Roadmap）

> 状态：动态规划文档，随决策更新。
> 定位：**个人创作者（小说创作 / 自娱）本地工具**——纯网页端，用户数据只存浏览器 IndexedDB，**不上服务器**（2026-08-21 决策）。
> 稳定设计依据：[DESIGN.md](./DESIGN.md) §2/§3/§7；验收证据归档：[docs/acceptance/RELEASE.md](./docs/acceptance/RELEASE.md)；文件定位：[docs/MAP.md](./docs/MAP.md)；机制与 SOP：[docs/KNOWLEDGE_BASE.md](./docs/KNOWLEDGE_BASE.md)。

## 0. 边界（明确不做）

- **AI 生成与辅助功能**（DESIGN §1）
- **多人实时协作**（DESIGN §1）
- **用户数据上云 / V2 FastAPI 后端**（2026-08-21 决策：数据不上服务器；Repository 抽象层接缝保留不废，见 §4）

## 1. 规划原则

- **一版本一主题**；近期条目带 AC 编号（`<vX.Y-Fn>` 格式）与通过标准；
- 远期候选只写方向 + 触发条件，不承诺排期；
- 验收证据统一归档到 RELEASE.md 附录 B（不另起归档体系）。

## 2. 近期排期（已确定，可验收）

| 版本 | 主题 | 内容概要 |
|------|------|----------|
| v2.4 | 数据生态 | Markdown 工作区导出（优先）→ 备份提醒 → PDF 导出（降优先级，可顺延） |
| v2.5 | 全局搜索 | 侧栏常驻搜索框，跨角色/百科/事件 |
| v2.6 | 复杂历法 | 非线性历法对齐；**可能触发数据格式 schemaVersion 3** |
| v2.7 | 技术债收尾 | ESLint 清零、覆盖率补齐、v2.4–v2.7 验收归档 |

### v2.4 数据生态

**v2.4-F1（优先）Markdown 工作区导出（纯文本格式）**
- 项目导出为一个文件夹：每个实体一个 `.md`（角色 / 百科 / 事件），`[[条目名]]` 链接语法保留，assets 图片按相对路径引用，可选 frontmatter 元数据（类型 / 颜色 / 历法时间）
- 产出可被 Obsidian / Typora / VS Code 等**任意纯文本工具**打开编辑
- 通过标准：逐实体 md 文件断言（链接 / 图片引用路径 / 时间字段）；zip 导出布局不受影响；导出的 md 可读回校验内容一致

**v2.4-F2 备份提醒（提醒而非自动化）**
- 距上次 zip 导出超过 N 天（默认 7，可配置）时，在导出中心/侧栏给出提醒，一键导出
- **不做自动化快照/回滚**（完整版已明确暂缓，见 §4）
- 通过标准：阈值触发/不触发的边界断言（纯函数可单测）

**v2.4-F3 PDF 导出（降优先级）**
- 范围：**角色卡 PDF**（Markdown → HTML → 打印样式路径）
- 时间线/图谱保持现有 PNG 导出；若 v2.4 预算不足，F3 顺延至后续版本并重新评估
- 通过标准：角色卡 PDF 生成成功、双主题样式正确、无截断

### v2.5 全局搜索

- **F1** 侧栏顶部**常驻搜索框**（明确不做快捷键呼出）
- 范围：角色全文（复用 `serializeBlocksText`）/ 百科（标题 + 正文 + 属性）/ 事件（标题 + 描述 + 参与者名）
- 交互：结果按类型分组展示，点击跳转对应视图并定位实体；失效引用实体显示占位（G5）
- 通过标准：三类实体命中/未命中断言（Vitest）；E2E 搜索 → 跳转定位链路

### v2.6 复杂历法

- 目标：支持**非线性历法对齐**（闰月 / 跳跃年 / 不规则纪年与通用纪年的对齐规则），事件排序与 display 正确
- 影响面：`utils/calendar.ts` 换算、序位轴（timelineOrder）排序、fork 边界（fork.ts）——时间线核心语义
- **风险点：可能触发数据格式变更（schemaVersion 2 → 3）**，届时执行 KNOWLEDGE_BASE §3 迁移 SOP（types + schemas + legacy + migration + zip 往返 + 主版本号）
- 通过标准：对齐换算 Vitest（含边缘年份）；序号轴与 fork 继承回归；zip 往返深比较；旧数据迁移用例

### v2.7 技术债收尾

- ESLint 8 条 unused-vars warning 清零
- coverage 补齐到门槛（storage / utils / schemas：lines 70% / branches 60%）
- **不投入 E2E 稳定性加固**（无实际 flaky 报告，48/48 稳定）
- 发布：全量门禁（test / coverage / check:tokens / lint / build / playwright）+ RELEASE.md 附录 B 归档 v2.4–v2.7

## 3. 远期候选（只写方向 + 触发条件，不排期）

| 候选 | 触发条件 |
|------|----------|
| 移动端 / 触屏适配（时间轴手势平移缩放、触屏拖拽、响应式布局） | 出现实际移动浏览/编辑需求 |

## 4. 已评估暂缓（记录决策，防止重复提案）

| 项 | 暂缓原因（2026-08-21 决策） |
|----|------------------------------|
| V2 FastAPI 云同步 / 数据上云 | **用户数据不上服务器**（明确边界）；Repository 抽象层与文件树 zip 布局保留接缝，将来若要恢复零成本 |
| 自动备份完整版（dexie 快照回滚） | 备份提醒（v2.4-F2）已覆盖低频事故场景 |
| 世界线对比视图（diff 两条线事件差异） | 当前创作流程无此需求，重新评估条件：时间线功能使用反馈 |
| 图谱纳入势力节点（DESIGN §2.3 承诺项） | 等待图谱实际使用反馈再动数据模型 |
| 快照页主题定制 | 快照当前形态够用 |
| 世界观 git 版本管理 | 依赖 V2 文件树后端，前提已暂缓 |

## 5. 关联文档

- [DESIGN.md](./DESIGN.md) §7 —— 演进路线原条目（本文件是其排期化与暂缓裁定）
- [DEV_PLAN.md](./DEV_PLAN.md) —— M/G 验收体系（本文件 AC 编号为其扩展）
- [docs/MAP.md](./docs/MAP.md) / [docs/KNOWLEDGE_BASE.md](./docs/KNOWLEDGE_BASE.md) —— 文件定位与机制 SOP
- [CHANGELOG.md](./CHANGELOG.md) —— 逐版本落地记录

---

*维护约定：决策变化更新本文件并在 CHANGELOG 记「变更」；暂缓项恢复排期时移入 §2 并注明。*