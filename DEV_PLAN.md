# World OC Card · 开发计划（DEV_PLAN）

> 依据：DESIGN.md V1.3
> 版本：1.0（2026-08-20）
> 验收标准编号规则：`M{里程碑}-{类别}{序号}`，类别：**F** 功能 / **E** 边界 / **P** 性能 / **D** 数据 / **S** 风格
> 完成定义（DoD，适用于每个里程碑）：功能开发完毕 + 该里程碑全部 AC 通过 + 自动化测试绿 + 生产构建下验收。

---

## 1. 里程碑总览

| 里程碑 | 内容 | 依赖 | 预估（单人） |
|--------|------|------|--------------|
| M0 脚手架、数据模型与主题框架 | Vite/Vue3/TS/Pinia/Router/Naive UI/Lucide；双主题 token 体系 + 大理石纹理层；zod 全量 schema | — | 2–3 天 |
| M1 存储层与项目管理 | dexie 多表、脏实体保存、assets Blob 表、zip 导入导出、项目首页 | M0 | 3–4 天 |
| M2 角色模块 + 模板系统 | 块编辑器、角色列表、模板保存/加载/管理/分享 | M1 | 4–5 天 |
| M3 百科模块 | 条目 CRUD、[[双向链接]]、反向引用、属性模板 | M1, M2（模板） | 3 天 |
| M4 时间线与世界线 | 事件 CRUD（字符串纪年双模式）、世界线 fork、泳道式卡片时间线、草稿箱、软解析+手动排序 | M1 | 6–8 天（最重） |
| M5 关系图谱 | G6 渲染、关系类型管理、边编辑、过滤 | M2 | 3 天 |
| M6 导出与分享中心 | zip 备份 UI、角色卡 MD/PNG、图谱与时间轴 PNG、单文件 HTML 快照 | M2–M5 | 3–4 天 |
| M7 打磨与发布 | 引用完整性巡检、孤儿资产清理、快捷键、空态、性能与兼容性验收 | M0–M6 | 2–3 天 |

预估合计约 1 个月单人工作量，仅供排期参考。

---

## 2. 通用验收标准（全局生效，每个里程碑验收时一并检查）

| 编号 | 验收项 | 通过标准 |
|------|--------|----------|
| G1 | 生产模式验收 | 所有 AC 在 `npm run build` + 静态服务器预览下执行，非 dev 模式 |
| G2 | 控制台干净 | 验收操作全程浏览器控制台无 error（warning 允许，需记录） |
| G3 | 中文界面 | 所有 UI 文案为中文，无占位英文/硬编码 lorem |
| G4 | 数据不丢失 | 任意编辑后 1s 内刷新页面 / 关闭标签页，数据不丢（防抖 + flush on `visibilitychange`/`beforeunload`） |
| G5 | 失效引用不崩溃 | 任何引用目标缺失的场景，UI 显示"失效引用"占位，不抛错、不白屏 |
| G6 | 无 base64 内联 | 任一时刻持久化的 JSON 内容中 `data:image` 出现次数为 0（图片只存 assets） |
| G7 | 浏览器矩阵 | Chrome / Edge / Firefox 最新版通过该里程碑的手动冒烟 |
| G8 | 自动化测试 | `npm run test` 全绿；数据层与纯函数模块行覆盖率 ≥ 70% |
| G9 | 双主题 | 任意页面切换 dark/light **即时生效**（含 G6 图、时间轴、画布、快照预览），无刷新、无残留旧色；选择持久化（localStorage）；首次访问跟随系统 `prefers-color-scheme` |
| G10 | 颜色与图标纪律 | 自定义组件/图表配置无硬编码色值（`#hex`/`rgb(` 字面量仅允许出现在主题 token 定义文件），图表颜色运行时读 token；全应用图标仅来自 Lucide，无混用 |
| G11 | 对比度达标 | 两主题下正文文本对比度 ≥ 4.5:1（WCAG AA）；世界线/关系类型色与其所在背景 ≥ 3:1 |
| G12 | 字体回退 | 断网状态下页面正常渲染：UI 用系统无衬线、正文回退系统衬线，无 FOIT 阻塞 |
| G13 | 低饱和约束 | token 定义文件中全部 chrome 色 HSL 饱和度 S ≤ 40%（脚本校验，纳入 CI）；数据语义色（世界线/关系类型/条目标识）豁免 |

---

## 3. 里程碑详述

### M0 脚手架、数据模型与主题框架

**任务**：Vite + Vue3 + TS(strict) + Pinia + Router + Naive UI（按需引入）+ Lucide 初始化；**双主题 token 体系（§5.2 全表）+ 程序化大理石纹理层（feTurbulence，§5.3）**；按 DESIGN.md §2–§3 定义全部 TS 类型与 zod schema；ESLint + Vitest 初始化；路由骨架（7 个空页面）；CREDITS.md 建立。

**验收标准：**

| 编号 | 类别 | 验收项 | 通过标准 |
|------|------|--------|----------|
| M0-F1 | F | 工程可运行 | `npm run dev` 启动，7 个路由页面可切换；`npm run build` 成功 |
| M0-F2 | F | 类型即文档 | TS 类型与 zod schema 与 DESIGN.md §2.6/§3 字段一一对应，无遗漏实体 |
| M0-F3 | F | 骨架校验 | zod 对 FieldBlock 七种块的合法/非法样例各 ≥ 1 组通过/拒绝（含 group 嵌套 3 层、link 块引用格式） |
| M0-S1 | S | 双主题框架 | 空壳页 dark/light 切换即时生效并持久化；重启浏览器后保持；清 localStorage 后跟随系统（G9 全项） |
| M0-S2 | S | 大理石纹理层 | 纹理仅在装饰区域（底色/侧栏/卡片表面）呈现，透明度 0.03–0.08；正文占位区、表单内部为纯 surface 色；两主题各有一组纹理参数且切换正确 |
| M0-S3 | S | 图标接入 | Lucide 按需引入在 ≥ 3 处空壳页使用，stroke 2px，构建产物只含用到的图标 |
| M0-S4 | S | token 校验脚本 | `npm run check:tokens` 脚本校验：token 文件无 S > 40% 的 chrome 色、必需 token 齐全（G13 基础） |
| M0-E1 | E | 非法数据拒绝 | 畸形 JSON（未知块类型、缺 id、错误嵌套）校验失败且错误信息能定位到字段路径 |
| M0-P1 | P | 构建体积 | 首屏 chunk gzip ≤ 300KB（Naive UI 按需 + 图标 tree-shaking） |
| M0-P2 | P | 纹理性能 | 纹理层为单装饰层（fixed/单一容器），滚动与交互无超过 200ms 长任务 |

### M1 存储层与项目管理

**任务**：dexie 建表（§3.2 全部 8 张表）；LocalRepository 实现（loadProject 全量读 / saveEntities 脏写 / assets Blob 读写 / exportZip / importZip，fflate）；防抖 500ms + 脏集合 + flush 钩子；schemaVersion 迁移管道；项目新建/删除/重命名/列表首页。

**验收标准：**

| 编号 | 类别 | 验收项 | 通过标准 |
|------|------|--------|----------|
| M1-F1 | F | 项目管理 | 新建/重命名/删除项目，多项目共存，列表持久（刷新后仍在） |
| M1-F2 | F | zip 往返 | 导出 zip → 删库 → 导入同一 zip → 序列化深比较一致（忽略 updatedAt 类时间戳） |
| M1-F3 | F | zip 结构 | 解开 zip 与 §3.1 文件夹布局逐路径一致；assets 为二进制文件 |
| M1-F4 | F | 脏实体保存 | 修改单个角色，通过 dexie 写钩子断言：仅 characters 表 1 条记录被写，settings/relations/events 表零写入 |
| M1-F5 | F | 图片隔离 | 上传 3MB PNG 作头像 → assets 表为 Blob 记录，角色 JSON 仅含 assetId（配合 G6） |
| M1-F6 | F | 版本迁移 | 构造 schemaVersion=0 的旧 zip，导入后升级为当前版本且数据字段正确 |
| M1-E1 | E | 残缺导入 | 从 zip 中删去某 characters/*.json 再导入：项目可打开，引用该角色处显示"失效引用"占位（配合 G5） |
| M1-E2 | E | 防抖聚合 | 500ms 内连续修改 10 次同一角色，只触发 1 次持久化；修改 3 种不同实体，触发 3 条记录写入 |
| M1-E3 | E | 空态 | 无项目时首页有空态引导（新建/导入 zip 入口） |
| M1-P1 | P | 打开速度 | 模拟项目（200 角色 + 1000 事件 + 500 条目）loadProject 全量载入 < 1s |
| M1-P2 | P | 保存速度 | 同规模项目单实体保存（防抖触发）< 100ms 完成 |
| M1-D1 | D | 存储测试 | zip 往返、脏写断言、迁移管道均有 Vitest 用例（fake-indexeddb），覆盖 M1-F2/F4/F6 |

### M2 角色模块 + 模板系统

**任务**：角色列表页（搜索、标签筛选）；块编辑器（七种块增/删/排序，group 拖入拖出，图片块选资产，link 块选目标）；角色卡渲染视图；模板系统全套（§2.6）；内置示例模板 2–3 个。

**验收标准：**

| 编号 | 类别 | 验收项 | 通过标准 |
|------|------|--------|----------|
| M2-F1 | F | 块编辑 | 七种块均可增删、上下排序、group 嵌套≥3 层不破坏结构；卡片区与编辑区切换正确渲染 |
| M2-F2 | F | 固定字段 | 除名称外无任何固定字段 UI；名称必填，其余整卡可为空 |
| M2-F3 | F | 全文搜索 | 搜索命中任一块内文本（含 kv 的 key 与 value、表格单元格、分组标题） |
| M2-F4 | F | 标签块 | 标记 `flag:'tags'` 的 list 块条目出现在标签筛选器；未标记不出现 |
| M2-F5 | F | 模板-保存 | "存为模板"默认只留结构（值清空）；勾选"保留当前值"后值一并保存；两种产物导入 zod 均合法 |
| M2-F6 | F | 模板-加载 | 新建角色选模板起稿；编辑中"从模板插入"追加块且不动已有块 |
| M2-F7 | F | 模板-管理 | 重命名/删除/排序即时生效并持久；内置模板可删除 |
| M2-F8 | F | 模板-分享 | 单模板导出 `.template.json` → 导入另一项目 → payload 深比较一致 |
| M2-E1 | E | 删除级联 | 删除被事件/关系/link 块引用的角色：先列出全部引用处，确认后 participantIds、关系边、link 块一并清理，无残留失效引用 |
| M2-E2 | E | 大文本 | 10 万字 backstory 粘贴进 text 块，输入不卡顿（防抖生效），保存后重开完整无损 |
| M2-E3 | E | 空卡 | 空白角色（仅名称）在列表、搜索、导出 Markdown 中均正常 |
| M2-S1 | S | 卡片排版 | 正文（text 块渲染）为衬线字体 16px/1.75；卡片表面纹理符合 §5.3 区域规则；两主题下卡片可读性走查通过 |
| M2-P1 | P | 列表性能 | 200 角色下搜索过滤响应 < 100ms |
| M2-D1 | D | 模板测试 | 模板保存/加载/插入的纯逻辑（结构清空值、payload 合并）有 Vitest 用例 |

### M3 百科模块

**任务**：条目 CRUD 与类型侧栏（内置 + 自定义类型）；`[[名称]]` 双向链接编辑语法与渲染；反向引用面板；属性模板。

**验收标准：**

| 编号 | 类别 | 验收项 | 通过标准 |
|------|------|--------|----------|
| M3-F1 | F | 条目 CRUD | 六类内置类型 + 自定义类型均可建条目；侧栏按类型分组计数正确 |
| M3-F2 | F | 双向链接 | 正文 `[[名称]]` 渲染为可点击链接跳转目标条目；保存时名称解析为 UUID |
| M3-F3 | F | 反向引用 | 条目底部"被引用于"列出：其他条目正文、事件 locationId、角色 link 块，点击可跳转 |
| M3-F4 | F | 属性模板 | 百科模板预置属性键集 + 可选正文骨架；新建与插入两条路径可用（复用 M2 模板机制） |
| M3-E1 | E | 名称唯一 | 同项目内重名条目保存被阻止并提示（**名称全局唯一**，DESIGN.md 附录 A1）；`[[不存在]]` 显示为"创建此条目"占位链接 |
| M3-E2 | E | 删除级联 | 删除被引用条目：列出引用处（含正文链接），确认后链接转为失效占位（配合 G5） |
| M3-S1 | S | 条目标识色 | 条目 color 使用低饱和调色板选择器，两主题下与背景对比 ≥ 3:1（配合 G11） |
| M3-D1 | D | 解析测试 | `[[..]]` 解析为 UUID、重名拒绝、未创建占位三逻辑有 Vitest 用例 |

### M4 时间线与世界线（核心）

**任务**：事件 CRUD（时间双模式：纪年法四段式 / 自定义自由文本；参与者/势力；描述；百科关联）；世界线管理（颜色、显隐、abandoned 标记）；fork 逻辑；泳道式卡片时间线（横/纵双模式、顶部草稿箱、排序徽标、拖拽重排、分叉连线）；软解析 + 手动排序（`utils/branchOrder.ts` 纯函数）。

**验收标准：**

| 编号 | 类别 | 验收项 | 通过标准 |
|------|------|--------|----------|
| M4-F1 | F | 事件 CRUD | 事件含全部 §2.4.2 字段；编辑抽屉保存后时间线即时更新 |
| M4-F2 | F | 世界线 fork | 在任意事件上"从此处创建 IF 线"：新泳道出现、分叉连线、fork 点事件被记录 |
| M4-F3 | F | 继承展示 | 子线泳道显示 fork 点及之前的父线事件（继承样式区分），fork 点之后仅显示本线事件；父线此后新增事件**不**出现在子线 |
| M4-F4 | F | 多级分叉 | 孙线（子线再 fork）继承展示正确，形成 ≥ 3 级分叉树 |
| M4-F5 | F | ~~双视图同源~~ | **废弃（v3 移除画布视图）** |
| M4-F6 | F | 软解析排序 | calendar 模式 年/月/日 全数字者，按 (历名,年,月,日) 自动入列；不可解析与自定义模式落线尾，可解析事件同刻稳定相邻 |
| M4-F7 | F | 排序徽标 | 不可解析未拖 → "待排序"；拖拽重排后 → "手动序"；跨历名切换处 → "历法转接，请核对"（纯派生） |
| M4-F8 | F | 横纵切换 | 泳道横/纵可切换、默认值遵循 PC（横）/移动窄屏（纵）；偏好存 localStorage |
| M4-E1 | E | 删除世界线 | 删除子线需确认并级联删除其事件；**主世界线不可删除**（按钮禁用 + 理由提示） |
| M4-E2 | E | fork 点被删 | 子线的 fork 点事件被删除或变为草稿：子线标记"分叉点失效"但事件可访问、可重新指定分叉点，不崩溃 |
| M4-E3 | E | 草稿箱 | 未定时事件进顶部草稿箱、不出现在任何泳道；补全时间选线入列；已定时可"放回草稿箱" |
| M4-E4 | E | abandoned | 废弃线默认折叠淡显，可展开；fork 自废弃线给出提示 |
| M4-S1 | S | 世界线颜色 | 12 色低饱和调色板选择器（DESIGN.md §5.4）；自定义色值实时对比度提示；两主题下泳道与卡片着色走查通过 |
| M4-S2 | S | 图表换肤 | 时间线与图谱全部颜色来自 token / 世界线数据色，主题切换即时换肤无残留（配合 G9/G10） |
| M4-P1 | P | 时间线性能 | 1000 事件 + 5 条世界线：初始渲染 < 2s；拖拽/滚动无超过 200ms 长任务（Performance 面板） |
| M4-P2 | P | ~~画布性能~~ | **废弃（v3 移除画布视图）** |
| M4-D1 | D | 排序/继承测试 | branchOrder（可解析性/插入位/重编号/徽标/displayTime）与 fork 可见事件集合计算有 Vitest 用例 |

### M5 关系图谱

**任务**：G6 力导向图；关系类型管理（名称/颜色/方向）；边增删改；节点交互；类型过滤。

**验收标准：**

| 编号 | 类别 | 验收项 | 通过标准 |
|------|------|--------|----------|
| M5-F1 | F | 渲染与布局 | 全部角色为节点，关系为边；力导向布局稳定不抖动 |
| M5-F2 | F | 关系类型 | 自定义类型（名称/颜色/有无方向）增删改即时反映到图例与边样式 |
| M5-F3 | F | 边编辑 | 界面内直接创建/编辑/删除关系边（选择双方、类型、描述），写入 relations.json |
| M5-F4 | F | 交互 | 点击节点跳转角色卡；按类型过滤显隐；图例常驻 |
| M5-E1 | E | 边界 | 0 关系、0 角色（空态引导）；单向/双向边样式区分正确；自环关系（自己对自己）可建且渲染不异常 |
| M5-S1 | S | 图谱换肤 | 节点/边/背景色运行时读 token 与数据色，主题切换即时生效（配合 G9/G10）；边颜色对比度 ≥ 3:1 |
| M5-P1 | P | 性能 | 200 节点 + 1000 边初始渲染 < 2s，拖拽节点无超过 200ms 长任务 |

### M6 导出与分享中心

**任务**：导出中心页（汇总全部导出入口）；角色卡 Markdown 导出；角色卡/图谱/时间轴 PNG；单文件 HTML 快照生成。

**验收标准：**

| 编号 | 类别 | 验收项 | 通过标准 |
|------|------|--------|----------|
| M6-F1 | F | zip 备份 | 从导出中心完成 zip 导出，行为与 M1-F2/F3 一致（复用同一实现） |
| M6-F2 | F | 角色 Markdown | 导出含全部块结构（分组→小节、kv→表格、表格块→MD 表格、图片→文件引用附 assets），渲染预览一致 |
| M6-F3 | F | PNG 导出 | 角色卡、关系图谱、时间轴（当前视口）三类 PNG 导出成功，无截断、无黑块 |
| M6-F4 | F | HTML 快照 | 生成单文件 HTML：**断网 + file:// 双击打开**可正常浏览角色/百科/时间线三个 tab，数据与项目当前态一致 |
| M6-E1 | E | 快照健壮性 | 快照内含失效引用占位（配合 G5）；50 角色+300 事件项目快照体积 < 10MB 且打开 < 3s |
| M6-E2 | E | 空/部分导出 | 仅导出单条世界线或筛选后角色集的 PNG/MD 均可用 |
| M6-S1 | S | 导出主题 | PNG 按当前主题导出（含纹理质感）；HTML 快照内联双主题并可切换、离线可用系统字体回退（配合 G12） |

### M7 打磨与发布

**任务**：全局引用完整性巡检面板；孤儿资产（未被引用的图片）检测与清理；常用快捷键（新建、保存、切换视图）；空态与新手引导；性能复测与浏览器矩阵；静态部署验证；CREDITS.md 复核。

**验收标准：**

| 编号 | 类别 | 验收项 | 通过标准 |
|------|------|--------|----------|
| M7-F1 | F | 引用巡检 | 一键扫描并列出全部失效引用（类型/位置/跳转），可逐条处理 |
| M7-F2 | F | 孤儿资产 | 检测未被任何实体引用的 assets 并列出，确认后删除；导出 zip 默认不含孤儿资产 |
| M7-F3 | F | 快捷键 | 新建角色/事件、切换时间轴↔画布、保存（手动 flush）有快捷键且有说明入口 |
| M7-S1 | S | 素材合规 | CREDITS.md 完整登记全部第三方素材/字体/图标及许可；仓库内位图素材均 ≤100KB 且为 CC0/宽松许可 |
| M7-S2 | S | 动效约定 | 全应用过渡 150–250ms ease-out；系统开启 `prefers-reduced-motion` 时过渡关闭 |
| M7-P1 | P | 全量复测 | M1/M4/M5 全部性能 AC 在同一模拟数据集上复测通过 |
| M7-E1 | E | 部署验证 | 产物上传任一静态服务器（含子路径 base 配置）可正常路由（刷新非首页 404 校验） |
| M7-E2 | E | 破坏演练 | 导入 M1-E1 残缺 zip + 手动制造失效引用后，巡检面板全部捕获；全程无白屏（配合 G5） |

---

## 4. 测试策略

| 层 | 工具 | 范围 | 门槛 |
|----|------|------|------|
| 单元测试 | Vitest | zod schema、事件排序（branchOrder）、fork 可见事件集合计算、模板结构处理、zip 往返/迁移（fake-indexeddb） | 数据层+纯函数行覆盖 ≥ 70%（G8） |
| 静态检查 | 脚本 + ESLint | token 饱和度/完整性（G13）、硬编码色值扫描（G10） | 纳入 CI，失败即阻断 |
| E2E 冒烟 | Playwright | 一条主链路：新建项目 → 模板建角色 → 建百科条目并互链 → 建事件 → fork 出 IF 线 → 导出 zip | 每次 M 里程碑验收前跑通 |
| 手动验收 | 人工 | 全部 AC 清单（生产构建） | 逐项打钩归档 |

E2E 只做冒烟不做全量，功能细节以 AC 手动清单为准，避免维护成本失控。

---

## 5. 发布检查清单（Release）

- [ ] M0–M7 全部 AC 通过并归档（勾选记录存 `docs/acceptance/`）
- [ ] G1–G13 全部通过
- [ ] `npm run build` 产物部署验证（M7-E1）
- [ ] zip 备份 → 全新浏览器导入 → 数据一致的最终演练
- [ ] CREDITS.md 完整（M7-S1）
- [ ] README：功能简介、部署说明、数据格式说明（链接 DESIGN.md）

---

## 附录 A：对 DESIGN.md 的微调记录

| # | 微调 | 原因 |
|---|------|------|
| A1 | 百科条目名称由"同类型下唯一"改为**项目内全局唯一** | `[[条目名]]` 链接语法只含名称，跨类型重名会产生解析歧义；牺牲少量命名自由换取链接无歧义 |

---

## 附录 B：V1 发布后迭代的验收标准（v1.2.0 → v2.3.3）

V1 发布后的功能迭代不新增里程碑编号，验收标准以版本为单位记录在 [docs/acceptance/RELEASE.md 附录 B](./docs/acceptance/RELEASE.md)（每版本一行：变更 + 验收证据 + 对应 E2E/单测）。新增 E2E 用例沿用现有分组命名（`M4 同刻多事件`、`M5 点阵背景跟随视口平移/缩放`、`M5 页内新建关系曲线立即渲染` 等），全局标准 G1–G13 对迭代同样生效。

迭代期引入的行为性约束（升级为全局标准执行）：

- **G14 ~~时间轴缩放尺度限制~~**：**已废弃（v3 移除序位轴缩放/惯性，改用泳道拖拽排序）**
- **G15 ~~聚簇可交互性~~**：**已废弃（v3 移除聚簇——卡片泳道天然免遮挡）**
- **G16 图谱页内变更即时重绘**：页内新建/编辑/删除关系与类型切换必须立即反映到画布（E2E 截图字节比对 + 图例箭头断言），不允许"刷新后才可见"。
