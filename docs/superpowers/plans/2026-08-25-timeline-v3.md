# 时间线 v3（字符串纪年 × 卡片化分支泳道）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把时间线从"数值绝对纪元排序"重构为字符串纪年双模式（纪年法四段式 / 自定义自由文本）+ 世界线内显式 rank 排序 + 泳道式分支展示（横/纵可切换），并移除因果链、自由画布、日历实体。

**Architecture:** 事件时间底层全字符串；世界线内排序唯一真源为 `rank` 整数（软解析只算插入位）；可解析性、徽标、重编号全部收敛为纯函数 `utils/branchOrder.ts`；迁移走 migration.ts 步进 v2→v3（zip 导入侧增加 legacy v2 schema 容错）；TimelineView 重写为纯 DOM 网格泳道（横/纵 CSS 双模式），CanvasView/VueFlow 整体移除。

**Tech Stack:** Vue 3 + TS + Pinia + vue-router（hash）+ zod 4 strictObject + dexie + fflate；测试 Vitest（node + fake-indexeddb）、Playwright。

**Spec:** `docs/superpowers/specs/2026-08-25-timeline-v3-design.md`（计划自此文档论证；执行者两篇都要读）

## Global Constraints

- 版本：本次重构最终发布 `3.0.0`（破坏性数据格式）。中间任务提交**不逐次升版本**（单次重构一个发布），最后一次统一升；CHANGELOG 在 Task 8 追加（含 causalLinks 丢弃声明）。
- 实体 schema 一律 `z.strictObject`（zod 4：`z.strictObject`、顶层 `z.uuid` 是旧 API 别套）；未知字段拒绝。
- 颜色纪律：禁止在 tokens.css / utils/colors.ts / utils/snapshot.ts 之外写 hex/rgb 字面量；新 UI 颜色一律读 CSS token（utils/tokens.ts）或 `resolveDataColor`/`palettePick`（utils/colors.ts）。
- 图片绝不 base64 内联进 JSON；assets 走 Blob 表。
- 路由 hash history + vite `base: './'` 不变（M7-E1）。
- 不新增任何 npm 依赖（本次还要**移除 @vue-flow/\***）。
- Pinia 响应式 Proxy 不能直接写 IndexedDB——local.ts 的 put 前过 `plain()`（JSON 往返）；本计划不改 local.ts 但新增写路径必须复用它。
- 删除实体走 integrity.ts 级联函数；被波及实体逐一 `store.mark` 脏标记（坑 6）。
- IndexedDB dexie 表结构**不变**（events 表行 = 实体 + projectId）；只在实体字段层变化。
- **中间态说明**：Task 1 删掉旧 Calendar/EventTime 类型后，`vue-tsc` 会红到 Task 7 视图迁移完成；每个任务只保证 `npm run test` 绿 + 无 lint 新增 error。最终门禁在 Task 9 全量过。
- 测试纪律：branchOrder 纯函数全覆盖（数据层/纯函数 ≥70% 覆盖率门槛）；zip 往返 + 迁移测试保持原强度。

---

### Task 1: v3 实体类型 + zod schema + 全仓测试夹具对齐

**Files:**
- Modify: `src/types/index.ts`
- Modify: `src/schemas/index.ts`
- Delete: `src/utils/calendar.ts`、`src/utils/timelineOrder.ts`、`tests/calendar.test.ts`
- Modify: `tests/schemas.test.ts`、`tests/storage.test.ts`、`tests/fork.test.ts`、`tests/integrity.test.ts`、`tests/export.test.ts`（fixture 机械修复）

**Interfaces:**
- Consumes: 现有 `ProjectData`/`ProjectSettings` 结构（shape 变化见下）
- Produces: 新 `EventTime`（双模式 union）、新 `TimelineEvent`、`CURRENT_SCHEMA_VERSION = 3`、`calendarSchema` 降级为 legacy（保留导出，专供 legacy settings schema 引用）

- [ ] **Step 1: 写新类型（types/index.ts）**

```ts
export type EventTime =
  | { mode: 'calendar'; era: string; year: string; month: string; day: string }
  | { mode: 'custom'; text: string }

export interface TimelineEvent {
  id: UUID
  worldlineId: UUID | null      // null = 草稿
  time: EventTime | null        // null = 未定时草稿
  title: string
  description: string
  participantIds: UUID[]        // 角色 + 百科势力条目，合并一列
  relatedCodexIds: UUID[]       // 通用百科关联（取代 locationId）
  rank: number                  // 世界线内 0..n-1，顺序唯一真源
  manualPlaced: boolean         // 此事件位置曾被人为拖拽放置
  collapsed: boolean
  locked: boolean
}
```

删除：`Calendar` 接口、旧 `EventTime`、`ProjectSettings.calendars` 字段、`CURRENT_SCHEMA_VERSION = 2→3`。

- [ ] **Step 2: 写 schema（schemas/index.ts）**

```ts
export const eventTimeSchema = z.union([
  z
    .strictObject({ mode: z.literal('calendar'), era: z.string(), year: z.string(), month: z.string(), day: z.string() })
    .refine((t) => [t.era, t.year, t.month, t.day].some((v) => v.trim() !== ''), '纪年法时间至少填写一个字段'),
  z.strictObject({ mode: z.literal('custom'), text: z.string().min(1) }),
])

export const eventSchema = z.strictObject({
  id: uuid,
  worldlineId: uuid.nullable(),
  time: eventTimeSchema.nullable(),
  title: z.string().min(1),
  description: z.string(),
  participantIds: z.array(uuid),
  relatedCodexIds: z.array(uuid),
  rank: z.number().int().nonnegative(),
  manualPlaced: z.boolean(),
  collapsed: z.boolean(),
  locked: z.boolean(),
})

/** v2 旧格式事件（calendarId+value+display / locationId / causalLinks / canvasPos）——迁移管道 v2→v3 与 zip 导入容错的输入 */
export const legacyEventV2Schema = z.object({
  id: uuid, worldlineId: uuid,
  time: z.object({ calendarId: uuid, value: z.number(), display: z.string() }).nullable(),
  title: z.string().min(1), description: z.string(),
  participantIds: z.array(uuid), locationId: uuid.nullable(), causalLinks: z.array(uuid),
  canvasPos: z.object({ x: z.number(), y: z.number() }).optional(),
  collapsed: z.boolean(), locked: z.boolean(),
})

/** v2 旧格式 settings.json（含 calendars 字段，relationTypes 已为 arrow 三态）——zip 导入容错输入 */
export const legacySettingsV2Schema = z.object({
  calendars: z.array(calendarSchema),
  relationTypes: z.array(relationTypeSchema),
  codexTypes: z.array(codexTypeSchema),
  worldlines: z.array(worldlineSchema),
})
```

改动：`settingsSchema` 删除 `calendars` 键（strict）；`eventTimeSchema`/`eventSchema` 如上；`calendarSchema` 保留但加注释 `/** legacy：仅 v0–v2 输入/迁移管道使用 */`。

- [ ] **Step 3: 删除旧工具与测试，修全部夹具**

- 删除 `src/utils/calendar.ts`、`src/utils/timelineOrder.ts`、`tests/calendar.test.ts`。
- `tests/schemas.test.ts`：新增 `eventTimeSchema` 用例（calendar 全空被拒、至少一字段通过、custom 空文本被拒），并把旧 event 构造改为新形状；加 `legacyEventV2Schema`/`legacySettingsV2Schema` 可解析旧形状用例。
- `tests/storage.test.ts` / `tests/fork.test.ts` / `tests/integrity.test.ts` / `tests/export.test.ts`：事件 fixture 机械替换——`time: {calendarId..., value..., display...}` → `time: { mode:'calendar', era:'第三纪元', year:'217', month:'', day:'' }`（或 `{ mode:'custom', text:... }`）；删 `locationId`/`causalLinks`/`canvasPos`，加 `relatedCodexIds: []`、`rank: 0`、`manualPlaced: false`；`worldlineId` 保持非空。**不改断言语义**（语义断言归 Task 3/4）。

- [ ] **Step 4: 跑测试**

Run: `npm run test`
Expected: PASS（fork/integrity/storage/export/schemas 等全部绿；calendar.test 已删）

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "refactor(timeline): v3 实体类型与 schema（字符串纪年双模式，去 Calendar/causalLinks/canvasPos）"
```

---

### Task 2: branchOrder.ts 纯函数层（软解析 / 插入位 / 重编号 / 徽标 / displayTime）

**Files:**
- Create: `src/utils/branchOrder.ts`
- Test: `tests/branchOrder.test.ts`（新建，替代 calendar.test.ts 的覆盖）

**Interfaces:**
- Consumes: `EventTime`/`TimelineEvent`（Task 1）
- Produces（后续 Task 5/6/7 全部依赖，名字不得改）:
  - `parseStatus(time: EventTime | null): 'auto' | 'manual'` — calendar 且 年/月/日全可转数字（空串按 0）→ 'auto'；否则 'manual'
  - `timeBucket(time: EventTime | null): { era: string; year: number; month: number; day: number } | null` — 仅 'auto' 时非 null
  - `compareBuckets(a, b): number` — (era 词法, year, month, day)
  - `displayTime(time: EventTime | null): string` — null→''；custom→text；calendar→非空字段拼 `"第三纪元 217 年 3 月 15 日"`（空段跳过，era 与 year 间一空格）
  - `insertIndex(ordered: TimelineEvent[], time: EventTime): number` — 'manual'→末尾；'auto'→按 compareBuckets 线性扫描（同桶插在相等之后）
  - `applyOrder(events: TimelineEvent[], newOrderIds: string[]): { changed: { id: string; oldRank: number; newRank: number }[] }` — 按 newOrderIds 赋 rank 0..n-1（变异 events），返回 rank 变了的行
  - `badgeFor(event, prev, next): 'pending' | 'manual' | 'era-boundary' | null` — 可解析：仅当前一个可解析且历名不同→'era-boundary'；不可解析：`manualPlaced` → 'manual' 否则 'pending'

- [ ] **Step 1: 写失败测试（tests/branchOrder.test.ts）**

```ts
import { describe, expect, it } from 'vitest'
import type { EventTime, TimelineEvent } from '@/types'
import { applyOrder, badgeFor, displayTime, insertIndex, parseStatus, timeBucket, compareBuckets } from '@/utils/branchOrder'

const cal = (era = '第三纪元', year = '217'): EventTime => ({ mode: 'calendar', era, year, month: '', day: '' })
const custom = (text: string): EventTime => ({ mode: 'custom', text })
const ev = (id: string, time: EventTime | null, rank = 0, manualPlaced = false): TimelineEvent =>
  ({ id, worldlineId: 'w', time, title: id, description: '', participantIds: [], relatedCodexIds: [], rank, manualPlaced, collapsed: false, locked: false })

describe('parseStatus', () => {
  it('calendar 全数字 → auto（空字段按 0）', () => {
    expect(parseStatus(cal())).toBe('auto')
    expect(parseStatus(cal('星历', '3'))).toBe('auto')
  })
  it('非数字 token / custom / null → manual', () => {
    expect(parseStatus(cal('第三纪元', '第三世'))).toBe('manual')
    expect(parseStatus(custom('黑暗时代'))).toBe('manual')
    expect(parseStatus(null)).toBe('manual')
  })
})

describe('displayTime', () => {
  it('calendar 四段齐全', () => {
    expect(displayTime({ mode: 'calendar', era: '第三纪元', year: '217', month: '3', day: '15' })).toBe('第三纪元 217 年 3 月 15 日')
  })
  it('只填 era', () => expect(displayTime(cal('第三纪元'))).toBe('第三纪元'))
  it('custom 原文 / null 空串', () => {
    expect(displayTime(custom('黑暗时代'))).toBe('黑暗时代')
    expect(displayTime(null)).toBe('')
  })
})

describe('timeBucket / compareBuckets', () => {
  it('bucket 空段归 0，按 (era, y, m, d) 比较', () => {
    expect(timeBucket(cal('第三纪元', '3'))).toEqual({ era: '第三纪元', year: 3, month: 0, day: 0 })
    expect(compareBuckets(timeBucket(cal('第二纪元', '9'))!, timeBucket(cal('第三纪元', '1'))!)).toBeLessThan(0)
    expect(compareBuckets(timeBucket(cal('第三纪元', '1'))!, timeBucket(cal('第三纪元', '2'))!)).toBeLessThan(0)
    expect(compareBuckets(timeBucket(cal('第三纪元', '2'))!, timeBucket(cal('第三纪元', '2'))!)).toBe(0)
  })
})

describe('insertIndex', () => {
  it('manual 落末尾；auto 按桶插入；同桶插在相等之后', () => {
    const ordered = [ev('a', cal('第三纪元', '1'), 0), ev('b', cal('第三纪元', '3'), 1)]
    expect(insertIndex(ordered, cal('第三纪元', '2'))).toBe(1)
    expect(insertIndex(ordered, cal('第三纪元', '3'))).toBe(2) // 同桶置后
    expect(insertIndex(ordered, custom('黑暗时代'))).toBe(2)    // manual 末尾
    expect(insertIndex([], cal())).toBe(0)
  })
})

describe('applyOrder', () => {
  it('按新顺序重赋 rank 并报告变化', () => {
    const es = [ev('a', cal(), 0), ev('b', cal(), 1), ev('c', cal(), 2)]
    const changed = applyOrder(es, ['c', 'a', 'b'])
    expect(es.map((e) => [e.id, e.rank])).toEqual([['c', 0], ['a', 1], ['b', 2]])
    expect(changed.map((c) => [c.id, c.oldRank, c.newRank])).toEqual([['c', 2, 0], ['a', 0, 1], ['b', 1, 2]])
  })
})

describe('badgeFor', () => {
  it('不可解析未拖 → pending；拖过 → manual', () => {
    expect(badgeFor(ev('a', custom('黑暗时代')), null, null)).toBe('pending')
    expect(badgeFor(ev('a', custom('黑暗时代'), 0, true), null, null)).toBe('manual')
  })
  it('历名切换边界 → era-boundary（可解析且与前一个历名不同）', () => {
    expect(badgeFor(ev('b', cal('第四纪元', '1'), 1), ev('a', cal('第三纪元', '9'), 0), null)).toBe('era-boundary')
    expect(badgeFor(ev('b', cal('第三纪元', '1'), 1), ev('a', cal('第三纪元', '9'), 0), null)).toBeNull()
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/branchOrder.test.ts`
Expected: FAIL（`@/utils/branchOrder` 不存在）

- [ ] **Step 3: 实现（src/utils/branchOrder.ts）**

```ts
// 时间线 v3 排序纯函数（DESIGN v3 §3）：软解析可解析性 / 插入位 / 重编号 / 徽标 / 展示文本。
// rank 是线内顺序唯一真源；本文件只负责"怎么算"，不负责持久化。
import type { EventTime, TimelineEvent } from '@/types'

/** calendar 模式 年/月/日 全可转数字（空串按 0）→ auto；custom / 非数字 token / null → manual */
export function parseStatus(time: EventTime | null): 'auto' | 'manual' {
  if (!time || time.mode === 'custom') return 'manual'
  const nums = [time.year, time.month, time.day]
  return nums.every((v) => v === '' || Number.isFinite(Number(v))) ? 'auto' : 'manual'
}

export interface TimeBucket { era: string; year: number; month: number; day: number }

export function timeBucket(time: EventTime | null): TimeBucket | null {
  if (!time || time.mode !== 'calendar' || parseStatus(time) !== 'auto') return null
  const n = (v: string) => (v === '' ? 0 : Number(v))
  return { era: time.era, year: n(time.year), month: n(time.month), day: n(time.day) }
}

export function compareBuckets(a: TimeBucket, b: TimeBucket): number {
  if (a.era !== b.era) return a.era < b.era ? -1 : 1
  return a.year - b.year || a.month - b.month || a.day - b.day
}

/** 展示文本：非空字段以空格连接并带单位，如 "第三纪元 217 年 3 月 15 日" */
export function displayTime(time: EventTime | null): string {
  if (!time) return ''
  if (time.mode === 'custom') return time.text
  const parts: string[] = []
  if (time.era) parts.push(time.era)
  if (time.year) parts.push(`${time.year} 年`)
  if (time.month) parts.push(`${time.month} 月`)
  if (time.day) parts.push(`${time.day} 日`)
  return parts.join(' ')
}

/** 找插入位：manual → 末尾；auto → 按桶线性扫描（同桶插在相等之后，稳定） */
export function insertIndex(ordered: TimelineEvent[], time: EventTime): number {
  const bucket = timeBucket(time)
  if (parseStatus(time) === 'manual' || !bucket) return ordered.length
  for (let i = 0; i < ordered.length; i++) {
    const b = timeBucket(ordered[i].time)
    if (b && compareBuckets(bucket, b) < 0) return i
  }
  return ordered.length
}

/** 按新顺序重赋 rank 0..n-1（变异 events）；返回 rank 发生变化的行 */
export function applyOrder(events: TimelineEvent[], newOrderIds: string[]): { changed: { id: string; oldRank: number; newRank: number }[] } {
  const byId = new Map(events.map((e) => [e.id, e]))
  const changed: { id: string; oldRank: number; newRank: number }[] = []
  newOrderIds.forEach((id, newRank) => {
    const e = byId.get(id)
    if (!e) return
    if (e.rank !== newRank) changed.push({ id, oldRank: e.rank, newRank })
    e.rank = newRank
  })
  return { changed }
}

export type EventBadge = 'pending' | 'manual' | 'era-boundary' | null

/** 徽标派生（纯派生，重排后自动重算）：
 *  不可解析且未拖过 → pending；不可解析且拖过 → manual；
 *  可解析但与前一个事件历名不同 → era-boundary（历法转接，请核对）。 */
export function badgeFor(event: TimelineEvent, prev: TimelineEvent | null, next: TimelineEvent | null): EventBadge {
  if (parseStatus(event.time) !== 'auto') return event.manualPlaced ? 'manual' : 'pending'
  if (!prev) return null
  const a = timeBucket(prev.time)
  const b = timeBucket(event.time)
  if (a && b && a.era !== b.era) return 'era-boundary'
  return null
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run tests/branchOrder.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/utils/branchOrder.ts tests/branchOrder.test.ts
git commit -m "feat(timeline): branchOrder 纯函数层（软解析/插入位/重编号/徽标/displayTime）"
```

---

### Task 3: 迁移 v2→v3 + zip 导入容错（legacy schemas）

**Files:**
- Modify: `src/storage/migration.ts`
- Modify: `src/storage/zip.ts`
- Modify: `tests/storage.test.ts`（新增 v2 zip 迁移用例）

**Interfaces:**
- Consumes: `legacyEventV2Schema`/`legacySettingsV2Schema`（Task 1）、`migrateProject` 现有管道
- Produces: `STEPS[2] = v2toV3`；`parseZip` 对 v2 输入零丢失

- [ ] **Step 1: 写失败测试（tests/storage.test.ts 追加）**

```ts
// v2 全字段事件 → v3 迁移断言：历法名→era、数值→year；月历法→custom 保真；
// locationId→relatedCodexIds；causalLinks/canvasPos 丢弃；旧草稿 worldlineId 置 null；
// rank 按旧绝对纪元推导；settings.calendars 删除。
// 注意：Calendar 类型已删——测试里用内联字面量（局部接口），勿 import Calendar。
```

（测试内联旧形状字面量，构造一个含 2 条世界线 4 个事件的 v2 `ProjectData` 形状对象 + v2 settings（含 2 个历法：年历 '第三纪元' offset 100 unitYears 1；月历 '星历' offset 0 unitYears 1/12），调 `migrateProject`，断言：

- 年历事件 → `{ mode:'calendar', era:'第三纪元', year:'217', month:'', day:'' }`
- 月历事件 → `{ mode:'custom', text: oldDisplay }`（display 非空时）
- 旧草稿（time:null）→ `worldlineId === null && time === null`
- `locationId` 并入 `relatedCodexIds`；迁移结果对象上无 `causalLinks` 键
- `settings.calendars` 为空数组
- 两条线 rank 分别 0..n-1 且排序与旧绝对纪元一致（同刻保持原数组顺序）
- warnings 含 v3 与因果移除声明文案）

再补 zip 侧用例：`buildZip` 产出的 bytes 无法直接回退旧格式——用 `zipSync` 手工构造 v2 形状 zip（settings 含 calendars、events 为旧字段形状、project.json schemaVersion=2），`parseZip(bytes)` → 迁移成功、事件数不丢、warnings 含迁移说明、`data.meta.schemaVersion === 3`。

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/storage.test.ts`
Expected: FAIL（legacy settings 带 calendars 被 strict 拒 / 迁移不识别 v2）

- [ ] **Step 3: 实现 migration.ts**

```ts
import { legacyEventV2Schema } from '@/schemas'
import type { TimelineEvent } from '@/types'
import type { z } from 'zod'
type LegacyEventV2 = z.infer<typeof legacyEventV2Schema>

interface LegacyCalendar { id: string; name: string; offset: number; unitYears: number }

/** v2 → v3：数值纪元时间 → 字符串纪年双模式；locationId 并入 relatedCodexIds；
 *  causalLinks/canvasPos 丢弃；旧草稿置 worldlineId=null；rank 按旧绝对纪元逐线推导。 */
function v2toV3(data: ProjectData, warnings: string[]): ProjectData {
  const settings = data.settings as unknown as { calendars: LegacyCalendar[] }
  const calById = new Map(settings.calendars.map((c) => [c.id, c]))
  const oldEvents = data.events as unknown as LegacyEventV2[]

  // rank：逐线按旧绝对纪元（value×unitYears+offset）升序；同刻保持原数组顺序
  const rankOf = new Map<string, number>()
  const byLine = new Map<string, LegacyEventV2[]>()
  for (const e of oldEvents) {
    if (!e.time) continue
    if (!byLine.has(e.worldlineId)) byLine.set(e.worldlineId, [])
    byLine.get(e.worldlineId)!.push(e)
  }
  for (const [lineId, evs] of byLine) {
    const abs = (e: LegacyEventV2) => {
      const c = e.time ? calById.get(e.time.calendarId) : undefined
      return e.time ? e.time.value * (c?.unitYears ?? 1) + (c?.offset ?? 0) : 0
    }
    const sorted = [...evs].sort((a, b) => abs(a) - abs(b))
    sorted.forEach((e, i) => rankOf.set(e.id, i))
  }

  const events: TimelineEvent[] = oldEvents.map((e) => {
    let time: TimelineEvent['time'] = null
    if (e.time) {
      const c = calById.get(e.time.calendarId)
      time = c && c.unitYears < 1
        ? { mode: 'custom', text: e.time.display || `${c.name} ${e.time.value} 月` }
        : { mode: 'calendar', era: c?.name ?? '', year: String(e.time.value), month: '', day: '' }
    }
    return {
      id: e.id,
      worldlineId: e.time ? e.worldlineId : null,
      time,
      title: e.title,
      description: e.description,
      participantIds: e.participantIds,
      relatedCodexIds: e.locationId ? [e.locationId] : [],
      rank: e.time ? (rankOf.get(e.id) ?? 0) : 0,
      manualPlaced: false,
      collapsed: e.collapsed,
      locked: e.locked,
    }
  })
  delete (data.settings as Record<string, unknown>).calendars
  warnings.push('数据已从 v2 迁移到 v3（数值纪年 → 字符串纪年；因果连线 causalLinks 已移除）')
  return { ...data, settings: data.settings, events }
}

const STEPS: Record<number, (d: ProjectData, w: string[]) => ProjectData> = {
  0: v0toV1,
  1: v1toV2,
  2: v2toV3,
}
```

- [ ] **Step 4: 实现 zip.ts 容错（v2 输入不丢失）**

settings 解析改为三级回退；events 目录改为新旧双 schema：

```ts
const settingsR = parseWith(settingsSchema, settingsJson)
const legacyV2SettingsR = settingsR.ok ? null : parseWith(legacySettingsV2Schema, settingsJson)
const legacySettingsR = legacyV2SettingsR?.ok ? null : parseWith(legacySettingsV1Schema, settingsJson)
let settingsData: ProjectData['settings']
if (settingsR.ok) settingsData = settingsR.data
else if (legacyV2SettingsR?.ok) settingsData = legacyV2SettingsR.data as unknown as ProjectData['settings']
else if (legacySettingsR?.ok) settingsData = legacySettingsR.data as unknown as ProjectData['settings']
else throw new Error(`settings.json 校验失败：${settingsR.error}`)
```

```ts
readDir('events', (path, json) => {
  const r = parseWith(eventsFileSchema, json)
  if (r.ok) { events.push(r.data.event); return }
  const legacy = parseWith(legacyEventV2Schema, json)   // v0–v2 旧格式，交迁移管道
  if (legacy.ok) { events.push(legacy.data as unknown as ProjectData['events'][number]); return }
  warnings.push(`${path} 校验失败已跳过：${r.error}`)
})
```

import 行补 `legacySettingsV2Schema`、`legacyEventV2Schema`。

- [ ] **Step 5: 跑测试**

Run: `npx vitest run tests/storage.test.ts tests/schemas.test.ts`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add src/storage/migration.ts src/storage/zip.ts tests/storage.test.ts
git commit -m "feat(storage): 迁移管道 v2→v3 与 zip 导入旧格式容错"
```

---

### Task 4: integrity v3 化（参与者含势力 / relatedCodexIds / 去因果）

**Files:**
- Modify: `src/utils/integrity.ts`
- Modify: `tests/integrity.test.ts`

**Interfaces:**
- Consumes: `ProjectData` v3 形状（Task 1）
- Produces: `RefHit.kind` 去掉 `'causal-link'`、`'event-location'`，新增 `'event-related'`；`scanBrokenReferences` 参与者校验 = 角色 ∪ 势力条目

- [ ] **Step 1: 写失败测试（tests/integrity.test.ts）**

- `codexReferences`：条目被事件 `relatedCodexIds` 引用 → hit `{ kind: 'event-related' }`；被事件 `participantIds`（势力）引用 → hit `{ kind: 'event-participant' }`；旧 `event-location` 断言删除。
- `eventReferences`：删除 causal-link 断言，保留 fork-point / link-block。
- `removeCodexCascade`：从事件 `participantIds`、`relatedCodexIds` 中清除该条目 id。
- `removeEventCascade`：不再有任何 causalLinks 清理（保留 fork 点置空与 link 块清理）。
- `scanBrokenReferences`：草稿（worldlineId null）不报"所属世界线不存在"；`relatedCodexIds` 中不存在条目 → 报错；`participantIds` 中：不存在或非势力条目 → 报错；势力判定 = settings.codexTypes 里 key==='faction' 的类型下辖条目。

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/integrity.test.ts`
Expected: FAIL

- [ ] **Step 3: 实现（integrity.ts）**

```ts
export interface RefHit {
  kind: 'event-participant' | 'relation' | 'link-block' | 'event-related' | 'fork-point'
  where: string
  id: string
}
```

```ts
/** 势力条目 id 集合：codexTypes 中 key==='faction' 的类型下辖条目 */
function factionIds(data: ProjectData): Set<string> {
  const factionTypeIds = new Set(data.settings.codexTypes.filter((t) => t.key === 'faction').map((t) => t.id))
  return new Set(data.codex.filter((c) => factionTypeIds.has(c.typeId)).map((c) => c.id))
}
```

- `codexReferences`：`e.locationId === entryId` 循环替换为

```ts
for (const e of data.events) {
  if (e.participantIds.includes(entryId)) hits.push({ kind: 'event-participant', where: `事件「${e.title}」的参与者`, id: e.id })
  if (e.relatedCodexIds.includes(entryId)) hits.push({ kind: 'event-related', where: `事件「${e.title}」的百科关联`, id: e.id })
}
```

- `eventReferences`：删 causalLinks 循环。
- `removeCodexCascade`：`if (e.locationId === entryId) e.locationId = null` 替换为

```ts
for (const e of data.events) {
  e.participantIds = e.participantIds.filter((p) => p !== entryId)
  e.relatedCodexIds = e.relatedCodexIds.filter((r) => r !== entryId)
}
```

- `removeEventCascade`：删 causalLinks 循环。
- `scanBrokenReferences` 事件段替换为：

```ts
const factions = factionIds(data)
for (const e of data.events as TimelineEvent[]) {
  if (e.worldlineId && !worldlineIds.has(e.worldlineId)) out.push({ type: '事件', where: `事件「${e.title}」`, detail: '所属世界线不存在' })
  for (const p of e.participantIds) {
    if (!charIds.has(p) && !factions.has(p)) out.push({ type: '事件', where: `事件「${e.title}」`, detail: `参与者 ${p} 不存在或非势力条目` })
  }
  for (const r of e.relatedCodexIds) if (!codexIds.has(r)) out.push({ type: '事件', where: `事件「${e.title}」`, detail: `百科关联 ${r} 不存在` })
}
```

删除局部 `calendarIds` 与历法检查。

- [ ] **Step 4: 跑测试**

Run: `npx vitest run tests/integrity.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/utils/integrity.ts tests/integrity.test.ts
git commit -m "refactor(timeline): integrity 适配 v3（势力参与者/relatedCodexIds，去 causalLinks/locationId）"
```

---

### Task 5: stores/project.ts 动作 + snapshot.ts 展示文本

**Files:**
- Modify: `src/stores/project.ts`
- Modify: `src/utils/snapshot.ts`

**Interfaces:**
- Consumes: `branchOrder.ts`（Task 2）、`integrity.ts`（Task 4）
- Produces（Task 6/7 依赖，名字不得改）:
  - `store.setEventTime(eventId: string, time: EventTime, worldlineId: string): void` — 置时间+入线；'auto' 按 `insertIndex` 插位，'manual' 落线尾，全线段 `applyOrder` 重编号并逐事件 mark
  - `store.moveToWorldline(eventId: string, targetWorldlineId: string): void` — 已定时换线：改写 worldlineId，目标线尾插入并重编号+mark
  - `store.moveToDraft(eventId: string): void` — 清 time/worldlineId，rank=0；原线重编号+mark
  - `store.reorderBranch(worldlineId: string, newOrderIds: string[]): void` — `applyOrder` 重排；其中不可解析且 rank 变者 `manualPlaced = true`；逐事件 mark
  - `store.removeEvent(eventId: string): void` — `removeEventCascade` + 原线重编号 + mark
  - `src/utils/snapshot.ts`：`display: e.time?.display ?? ''` → `display: displayTime(e.time)`

- [ ] **Step 1: 实现 store 动作（src/stores/project.ts）**

```ts
function reindexBranch(worldlineId: string | null): void {
  if (!worldlineId) return
  const branch = current.value.events.filter((e) => e.worldlineId === worldlineId)
  const order = [...branch].sort((a, b) => a.rank - b.rank).map((e) => e.id)
  applyOrder(branch, order) // 删除/换线后可能出现空洞，统一重编号
  for (const e of branch) mark({ kind: 'event', id: e.id })
}

function setEventTime(eventId: string, time: EventTime, worldlineId: string): void {
  const ev = current.value.events.find((e) => e.id === eventId)
  if (!ev) return
  const branch = current.value.events.filter((e) => e.worldlineId === worldlineId && e.id !== eventId)
  const order = [...branch].sort((a, b) => a.rank - b.rank).map((e) => e.id)
  const idx = insertIndex(order.map((id) => current.value.events.find((x) => x.id === id)!), time)
  order.splice(idx, 0, eventId)
  ev.time = time
  ev.worldlineId = worldlineId
  applyOrder([...branch, ev], order)
  for (const b of [...branch, ev]) mark({ kind: 'event', id: b.id })
}

function moveToDraft(eventId: string): void {
  const ev = current.value.events.find((e) => e.id === eventId)
  if (!ev) return
  const oldLine = ev.worldlineId
  ev.time = null
  ev.worldlineId = null
  ev.rank = 0
  mark({ kind: 'event', id: eventId })
  if (oldLine) reindexBranch(oldLine)
}

function moveToWorldline(eventId: string, targetWorldlineId: string): void {
  const ev = current.value.events.find((e) => e.id === eventId)
  if (!ev || !ev.time) return
  const oldLine = ev.worldlineId
  ev.worldlineId = targetWorldlineId
  const branch = current.value.events.filter((e) => e.worldlineId === targetWorldlineId)
  const order = [...branch].sort((a, b) => a.rank - b.rank).map((e) => e.id)
  ev.rank = order.length // 落尾
  for (const b of branch) mark({ kind: 'event', id: b.id })
  if (oldLine) reindexBranch(oldLine)
}

function reorderBranch(worldlineId: string, newOrderIds: string[]): void {
  const branch = current.value.events.filter((e) => e.worldlineId === worldlineId)
  const { changed } = applyOrder(branch, newOrderIds)
  for (const c of changed) {
    const e = branch.find((x) => x.id === c.id)!
    if (parseStatus(e.time) !== 'auto') e.manualPlaced = true
    mark({ kind: 'event', id: e.id })
  }
}

function removeEvent(eventId: string): void {
  const ev = current.value.events.find((e) => e.id === eventId)
  removeEventCascade(current.value, eventId)
  if (ev?.worldlineId) reindexBranch(ev.worldlineId)
}
```

加入 return 列表（现有 `upsertEvent` 等保留）。注意：`setEventTime` 里 `insertIndex` 的 ordered 参数要求 `TimelineEvent[]`，用 map 取实体（如上）。

- [ ] **Step 2: snapshot.ts 替换展示字段**

```ts
// 顶部 import：import { displayTime } from '@/utils/branchOrder'
display: displayTime(e.time),
```
（原 `e.time?.display ?? ''` 一处；其余结构不动）

- [ ] **Step 3: 跑测试 + 编译核对**

Run: `npm run test`
Expected: PASS（snapshot 无单测，靠 export/storage 相关性绿；vue-tsc 此阶段仍红属预期）

- [ ] **Step 4: 提交**

```bash
git add src/stores/project.ts src/utils/snapshot.ts
git commit -m "feat(timeline): store 事件动作（补时间/换线/放回草稿/重排/删除）+ 快照展示文本"
```

---

### Task 6: EventDrawer 时间编辑区 + 参与者/百科关联选择器

**Files:**
- Create: `src/components/timeline/EventTimeEditor.vue`
- Modify: `src/components/timeline/EventDrawer.vue`

**Interfaces:**
- Consumes: `displayTime`（Task 2）、store 新动作（Task 5）
- Produces: 时间双模式表单组件（后续 TimelineView 复用）

- [ ] **Step 1: EventTimeEditor.vue（双模式表单）**

```vue
<template>
  <n-radio-group v-model:value="mode" size="small">
    <n-radio-button value="none">未定时（草稿）</n-radio-button>
    <n-radio-button value="calendar">纪年法</n-radio-button>
    <n-radio-button value="custom">自定义</n-radio-button>
  </n-radio-group>
  <div v-if="mode === 'calendar'" class="tm-fields">
    <n-input v-model:value="era" placeholder="历名，如：第三纪元" />
    <n-input v-model:value="year" placeholder="年" />
    <n-input v-model:value="month" placeholder="月" />
    <n-input v-model:value="day" placeholder="日" />
  </div>
  <n-input v-else-if="mode === 'custom'" v-model:value="text" placeholder="自由填写，如：黑暗时代的中叶" />
</template>

<script setup lang="ts">
// props: modelValue: EventTime | null；emits: ['update:modelValue']
// 内部 state 由 modelValue 初始化；mode: modelValue 为 null → 'none'；calendar → 'calendar'；custom → 'custom'
// 变更时 emit：mode==='none' → null；'calendar' → { mode:'calendar', era, year, month, day }（允许全空，提交时由调用方拦）
//                   'custom' → { mode:'custom', text }
</script>
```

（n-input 四段式用 `.tm-fields` 网格排布；全部显式 import Naive UI 组件——项目未开自动导入。时间字段全是字符串，无数字校验。）

- [ ] **Step 2: EventDrawer.vue 改造**

- 替换时间编辑区：`<EventTimeEditor v-model="editingTime" />`。
- 保存逻辑（原 `save` 路径）：
  - `editingTime` 未定→已定时（原来 time==null）：调 `store.setEventTime(event.id, editingTime, worldlineId)`（新增"所属世界线"n-select，选项 = 全部世界线；补全时间时必选，默认主世界线）。
  - 已定时：时间变化 → `upsertEvent({ ...event, time })`；所属线变化 → `store.moveToWorldline(event.id, newLineId)`。
  - "放回草稿箱"按钮（仅已定时且 drawer 处于编辑态）：`store.moveToDraft(event.id)` 并关闭抽屉。
  - `locationId` 字段（原地点选择）删除。
- 参与者选择器：`participantIds` 选项 = 角色 + 势力条目（`data.codex.filter(c => factionTypeIds.has(c.typeId))`，label 加后缀 `(势力)`），n-select multiple。
- 新增"关联百科"选择器：`relatedCodexIds`，选项 = 全部 codex 条目，multiple。

- [ ] **Step 3: 本地验证**

Run: `npm run dev`，手工过一遍：未定时↔纪年法↔自定义切换；补时间选线；放回草稿箱；参与者选角色与势力；百科关联多选。`npx vue-tsc --noEmit` 仍允许红（TimelineView 未迁移）。

- [ ] **Step 4: 提交**

```bash
git add src/components/timeline/EventTimeEditor.vue src/components/timeline/EventDrawer.vue
git commit -m "feat(timeline): 事件抽屉双模式时间编辑与参与者/百科关联选择器"
```

---

### Task 7: TimelineView 重写 + 删除面（CanvasView/router/@vue-flow）

**Files:**
- Rewrite: `src/views/TimelineView.vue`
- Delete: `src/views/CanvasView.vue`
- Modify: `src/router/index.ts`（删 canvas 路由）、`package.json`（删 @vue-flow/core、@vue-flow/background、@vue-flow/controls，npm install 同步 lock）
- Modify: `src/views/GraphView.vue` 等（`rg -l "canvas" src --type vue` 核对，如引用画布跳转则清）

**Interfaces:**
- Consumes: `branchOrder.ts`（badgeFor/displayTime）、store 新动作（Task 5）、`EventTimeEditor`（Task 6）
- Produces: 新时间线页（横/纵泳道 + 草稿箱 + 徽标 + DnD 重排 + fork 连线 + PNG 导出按钮沿用）

- [ ] **Step 1: 保留清单（从旧 TimelineView.vue 继承）**

世界线管理栏（chips/显隐/重命名/废弃/恢复/删除、从此处创建 IF 线、expandAbandoned）、导出 PNG、抽屉接线（`openDrawer`/selectedEvent）、`resolveDataColor`/`palettePick` 取色、`lineColor`、`n-tag` 分支标识。

- [ ] **Step 2: 核心计算与结构（新文件骨架）**

```ts
const orientation = ref<'h' | 'v'>(
  (localStorage.getItem('timelineOrientation') as 'h' | 'v') ?? (window.matchMedia('(max-width: 767px)').matches ? 'v' : 'h'),
)
function toggleOrientation() {
  orientation.value = orientation.value === 'h' ? 'v' : 'h'
  localStorage.setItem('timelineOrientation', orientation.value)
}

// 世界线树：父线先于子线渲染（children 按 order 稳定）
const lanes = computed(() => {
  const byParent = new Map<string | null, Worldline[]>()
  for (const w of wls) {
    if (!byParent.has(w.parentWorldlineId)) byParent.set(w.parentWorldlineId, [])
    byParent.get(w.parentWorldlineId)!.push(w)
  }
  const out: { wl: Worldline; anchorEventId: string | null }[] = []
  const walk = (parentId: string | null) => {
    for (const w of byParent.get(parentId) ?? []) {
      out.push({ wl: w, anchorEventId: w.parentWorldlineId ? w.forkPointEventId : null })
      walk(w.id)
    }
  }
  walk(null)
  return out
})

// 线内排序事件 + 徽标（badgeFor 需要前驱/后继）
const branchEvents = (wlId: string) => {
  const es = store.events.filter((e) => e.worldlineId === wlId).sort((a, b) => a.rank - b.rank)
  return es.map((e, i) => ({ event: e, badge: badgeFor(e, es[i - 1] ?? null, es[i + 1] ?? null) }))
}

const drafts = computed(() => store.events.filter((e) => e.worldlineId === null))

const badgeText: Record<string, string> = { pending: '待排序', manual: '手动序', 'era-boundary': '历法转接，请核对' }
```

模板骨架（横/纵共用同一 DOM，靠 CSS class 控制流向）：

```vue
<div class="timeline-v3" :class="orientation">
  <div class="topbar panel">
    …世界线管理（沿用旧文件）…
    <n-button size="small" @click="toggleOrientation">
      <Columns v-if="orientation === 'v'" :size="14" /> 切换到{{ orientation === 'h' ? '纵向' : '横向' }}
    </n-button>
  </div>
  <div class="draft-box panel">
    <div class="draft-title">草稿箱（未定时）{{ drafts.length ? `· ${drafts.length}` : '' }}</div>
    <div class="draft-cards">
      <div v-for="d in drafts" :key="d.id" class="card draft" @click="openDrawer(d)">
        <div class="card-title">{{ d.title }}</div>
        <span class="badge pending">待排序</span>
      </div>
      <n-button size="small" type="primary" @click="addDraft"><Plus :size="14" /> 新建草稿事件</n-button>
    </div>
  </div>
  <div class="lanes panel">
    <div v-for="l in lanes" :key="l.wl.id" class="lane" :class="{ abandoned: l.wl.status === 'abandoned' }">
      <div class="lane-head">
        <span class="lane-dot" :style="{ background: lineColor(l.wl.color) }" />
        <span class="lane-name">{{ l.wl.name }}</span>
      </div>
      <div class="lane-cards">
        <div
          v-for="({ event, badge }, i) in branchEvents(l.wl.id)"
          :key="event.id"
          class="card"
          :class="{ locked: event.locked, collapsed: event.collapsed }"
          draggable="true"
          @click="openDrawer(event)"
          @dragstart="onDragStart($event, l.wl.id, event.id)"
          @dragover.prevent
          @drop.prevent="onDrop($event, l.wl.id, event.id)"
        >
          <div class="card-title">{{ event.title }}</div>
          <div class="card-time">{{ displayTime(event.time) }}</div>
          <span v-if="badge" class="badge" :class="badge">{{ badgeText[badge] }}</span>
        </div>
      </div>
    </div>
  </div>
</div>
```

fork 连线：`.lane` 容器相对定位，锚点线（`l.anchorEventId`）用 SVG `<line>` 覆盖层连接——横向从父线卡片右缘到本线首卡片左缘；纵向从父线卡片下缘到本线首卡片上缘；坐标由 `getBoundingClientRect` + `ResizeObserver` 计算缓存在 map 中。

DnD（HTML5）：

```ts
const dragState = ref<{ fromLine: string; eventId: string } | null>(null)
function onDragStart(e: DragEvent, lineId: string, eventId: string) {
  dragState.value = { fromLine: lineId, eventId }
  if (e.dataTransfer) e.dataTransfer.effectAllowed = 'move'
}
function onDrop(e: DragEvent, lineId: string, targetEventId: string) {
  const d = dragState.value
  if (!d || d.eventId === targetEventId) return
  const order = branchEvents(lineId).map((x) => x.event.id)
  order.splice(order.indexOf(d.eventId), 1)
  order.splice(order.indexOf(targetEventId), 0, d.eventId) // 插到目标之前
  store.reorderBranch(d.fromLine, order)
  dragState.value = null
}
```

- `addDraft`：`store.upsertEvent({ id: uuid(), worldlineId: null, time: null, title: '未命名事件', description: '', participantIds: [], relatedCodexIds: [], rank: 0, manualPlaced: false, collapsed: false, locked: false })` 后 `openDrawer(新事件)`。
- 旧交互（缩放/平移/惯性/聚簇/序位网格/全部事件列表）不迁移。草稿卡与线上卡共用 `openDrawer`（复用 Task 6 抽屉，含补时间/放回草稿箱）。
- 样式：`.timeline-v3.h` 下 `.lanes` 纵向堆叠、`.lane-cards` 横向 flex 流；`.timeline-v3.v` 下 `.lanes` 横向排布（每列一条线）、`.lane-cards` 纵向流。颜色全部走 CSS token（utils/tokens.ts），禁止 hex 字面量。

- [ ] **Step 3: 删除面**

- 删 `CanvasView.vue`；router 删 `/timeline/canvas`（导航按 meta.order 渲染，删后自动顺延）。
- `package.json` 依赖删除 `@vue-flow/core`、`@vue-flow/background`、`@vue-flow/controls`；`npm install` 同步 package-lock.json。
- `rg -n "canvas|VueFlow|@vue-flow" src` 应为空。

- [ ] **Step 4: 门禁**

Run: `npm run test && npm run build && npm run lint`
Expected: 三者全绿（vue-tsc 自本任务起恢复绿）

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat(timeline): 泳道式卡片时间线（横纵切换/草稿箱/排序徽标/拖拽重排），移除画布与 VueFlow"
```

---

### Task 8: 版本 3.0.0 + 文档回写

**Files:**
- Modify: `package.json`（version → 3.0.0）
- Modify: `CHANGELOG.md`（顶部追加，含破坏性变更声明）
- Modify: `DESIGN.md`（§2.4 全节重写为字符串纪年卡片模型；§2.4.4 双视图改为"单视图 + 草稿箱"；§3.1/§3.3 存储与校验说明、§4 路由表去 canvas、架构图去 Vue Flow）
- Modify: `DEV_PLAN.md`（时间线相关 AC：M4 系列事件字段、插入/排序、聚簇、画布相关 AC 标记废弃或改写为新行为）
- Modify: `docs/acceptance/RELEASE.md`（归档证据按新行为更新：时间线交互断言、画布条目移除；门禁证据 Task 9 后补，先标注）

- [ ] **Step 1: 改版本与 CHANGELOG**

CHANGELOG 顶部条目（格式照旧）：「3.0.0（2026-08-25）—— 破坏性：时间线改为字符串纪年卡片模型（纪年法/自定义双模式）；事件排序改为世界线内手动/软解析混合序；移除因果连线与画布视图；可选字段变更为参与者（含势力）与通用百科关联（原地点并入）」。

- [ ] **Step 2: 回写 DESIGN.md/DEV_PLAN.md/RELEASE.md**

按 spec §1 目标与 §2–§9 内容同步；删除 canvas 路由与 Vue Flow 描述；AC 编号维持原体系（改动内容、不重编号）。

- [ ] **Step 3: 提交**

```bash
git add -A
git commit -m "release: 3.0.0（时间线 v3 字符串纪年重构）+ 文档回写"
```

---

### Task 9: E2E 重写 + 全量门禁

**Files:**
- Modify: `e2e/smoke.spec.ts`、`e2e/verification.spec.ts`、`e2e/perf.spec.ts`
- Modify: `docs/acceptance/RELEASE.md`（补门禁证据）

- [ ] **Step 1: E2E 断言替换**

- `/#/timeline/canvas` 所有 goto → `/#/timeline` 或删除（perf.spec 的画布计时段删除）。
- smoke：`/#/timeline` 下断言 `.draft-box` 可见、`.lane` 数 = 世界线数、`.card` 存在且带 `.card-time` 文本（displayTime 产物）；切换横纵按钮存在且可点（`page.getByRole('button', { name: /切换/ })`）。
- verification：时间线关键路径——
  - 新建草稿事件（`.draft-box` 内按钮）→ 卡片出现于草稿箱且不出现于 `.lane-cards`；
  - 草稿补全时间（抽屉内选世界线 + 纪年法年字段）→ 卡片移入对应 `.lane`；
  - 已定时卡"放回草稿箱" → 回到 `.draft-box`；
  - DnD 重排用 `page.dispatchEvent`（HTML5 dataTransfer）驱动：`dragstart` → `drop` 于目标卡 → 两卡 DOM 顺序变化断言；
  - 横/纵切换后 `.timeline-v3` 的 class 含 `h`/`v`。
  - **无需 G6 拖拽，坑 9（Playwright 拖拽）不适用。**

- [ ] **Step 2: 全量门禁**

```bash
npm run lint            # 0 error
npm run check:tokens    # 通过（新 UI 颜色全部走 token）
npm run coverage        # 数据层/纯函数 ≥70%
npm run build           # vue-tsc + vite build 绿
npx playwright test     # Chromium/Firefox/Edge 三浏览器绿（本机未装浏览器先 npx playwright install）
```

- [ ] **Step 3: RELEASE.md 补证据 + 提交**

```bash
git add -A
git commit -m "test(e2e): 时间线 v3 E2E 重写与全量门禁证据（3.0.0）"
```

---

## Self-Review 记录（写完即查）

1. **Spec 覆盖**：双模式时间/软解析+徽标/rank 真源/按线排序/横纵切换/草稿箱双向/参与者合并/relatedCodexIds/持续时间砍掉/因果移除/CanvasView 移除/日历实体删除/迁移 v2→v3（含月历保真、locationId 并入、rank 推导、草稿置空、causalLinks 声明）/版本 3.0.0/文档回写/测试纪律 —— 全部落在 Task 1–9。spec §9 验收要点 → Task 2/3/5/7 测试与 E2E 覆盖。
2. **占位符扫描**：无 TBD/TODO；视图任务的骨架即真实代码 + 明确保留清单（重写场景给出结构而非逐行伪代码属预期）。
3. **类型一致**：`parseStatus/timeBucket/compareBuckets/displayTime/insertIndex/applyOrder/badgeFor` Task 2 定义、Task 5/7 消费，名字统一；store 动作 `setEventTime/moveToWorldline/moveToDraft/reorderBranch/removeEvent` Task 5 定义、Task 6/7 消费；`legacyEventV2Schema/legacySettingsV2Schema` Task 1 定义、Task 3 消费。
4. **已发现并修正的坑**：(a) settingsSchema strict 拒带 calendars 的 v2 settings → legacySettingsV2Schema 兜底（Task 1/3）；(b) strict eventSchema 拒 v2 事件文件 → legacyEventV2Schema 兜底（Task 1/3）；(c) `Calendar` 类型删除后迁移不能 import 它 → 用本地 `LegacyCalendar` 接口（Task 3）；(d) zod 4 用 `z.strictObject`，勿写旧 `z.object` 于新 schema（Task 1）；(e) 重编号波及整线 → 逐事件 mark（Task 5）；(f) `insertIndex` 入参为 `TimelineEvent[]`，store 需先取实体再传（Task 5）。