# v2.4 数据生态 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 ROADMAP §2 v2.4 三项：F1 Markdown 工作区导出（优先）、F2 备份提醒、F3 角色卡 PDF（可顺延）。

**Architecture:** F1 = 纯函数模块 `utils/workspace.ts`（实体→md 文件映射 + fflate zip），复用既有 `characterToMarkdown`（仅加两个可选渲染参数，默认行为不变）；F2 = 纯函数 `utils/backup.ts`（阈值判断）+ localStorage 戳 + 导出页横幅/侧栏圆点；F3 = `utils/printExport.ts`（marked→HTML→隐藏 iframe 打印），主题色运行时经 `utils/tokens.ts` 的 `readToken` 内联。

**Tech Stack:** Vue 3 + TS + Pinia（现有栈）；新增依赖：**无**（fflate / marked 均已在 package.json）。

**Spec:** [ROADMAP.md](../../../ROADMAP.md) §2「v2.4 数据生态」；数据模型见 [DESIGN.md](../../../DESIGN.md) §3。

## Global Constraints

- **不动 zip 项目格式**（DESIGN §3.1）：工作区导出走独立函数 `buildWorkspaceZip`，`src/storage/zip.ts` 的 `buildZip`/`parseZip` 一行不改，schemaVersion 保持 3。
- **颜色纪律（G10/G13）**：源码不得出现白名单外 hex/rgb 字面量；测试断言里的实体色值（如 `#8fae8b`）是**数据**不是 chrome 色，允许；组件样式只用 `var(--token)`。`npm run check:tokens` 必须过。
- **Naive UI 组件必须显式 import**（本项目无自动导入）。
- **版本纪律**：每个 Task 落地时递增 `package.json` version 并在 `CHANGELOG.md` 顶部追加条目（功能+次版本）。本计划内 F1→3.2.0、F2→3.3.0、F3→3.4.0。
- 测试跑 `npx vitest run tests/<file>`（node 环境 + fake-indexeddb，见 `tests/setup.ts`）。
- 提交信息用中文，格式 `feat|fix|chore: ...`。
- 所有新持久化走 localStorage（提醒是 UI 态，不是项目数据，**不进 dexie / 不进 zip**，避免 schema 变更）。

---

### Task 1: 扩展 mdExport 渲染选项（F1 地基）

**Files:**
- Modify: `src/utils/mdExport.ts`
- Test: `tests/export.test.ts`

**Interfaces:**
- Consumes: 现有 `characterToMarkdown(c, data, assets)`。
- Produces: `characterToMarkdown(c, data, assets, opts?: MdRenderOptions)`，`MdRenderOptions = { assetUrl?: (a: AssetMeta) => string; linkText?: (name: string) => string }`。默认行为完全不变（不传 opts 时输出与现在逐字节一致——M6-F2 既有测试守着）。

- [ ] **Step 1: 写失败测试**

在 `tests/export.test.ts` 的 `describe('M6-F2 角色 Markdown 导出')` 之后追加（`makeData` 已存在，直接复用；`d.characters[1]` 是 `角色1`，给 `角色0` 加一个指向它的有效链接块）：

```ts
describe('v2.4-F1 workspace 渲染选项', () => {
  it('assetUrl/linkText 选项：图片相对路径 + [[双链]]；不传选项时行为不变', () => {
    const d = makeData()
    const c0 = d.characters[0]
    c0.fieldBlocks.push({ type: 'link', title: '认识', targetType: 'character', targetId: 'c1' })
    const styled = characterToMarkdown(c0, d, assets, {
      assetUrl: (a) => `../assets/${a.id}.${a.ext}`,
      linkText: (n) => `[[${n}]]`,
    }).md
    expect(styled).toContain('![立绘](../assets/a1.png)')
    expect(styled).toContain('- 认识：[[角色1]]')
    // 失效引用不走 linkText，仍是纯文本
    expect(styled).toContain('- 挚友：失效引用')

    const plain = characterToMarkdown(c0, d, assets).md
    expect(plain).toContain('![立绘](assets/a1.png)')
    expect(plain).toContain('- 认识：角色1')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/export.test.ts`
Expected: FAIL——`characterToMarkdown` 第 4 参 `opts` 不存在（TS 报错或断言 `../assets/a1.png` 不命中）。

- [ ] **Step 3: 最小实现**

`src/utils/mdExport.ts`：

```ts
export interface MdRenderOptions {
  /** 图片引用路径（默认 assets/<id>.<ext>） */
  assetUrl?: (a: AssetMeta) => string
  /** 实体链接展示（默认纯名字；工作区导出传 [[名字]]） */
  linkText?: (name: string) => string
}
```

`blockToMd` 的 `ctx` 参数加两个可选字段（同形状），`image` 分支改：

```ts
const url = ctx.assetUrl ? ctx.assetUrl(asset) : `assets/${asset.id}.${asset.ext}`
lines.push('', `![${b.title || '图片'}](${url})`)
```

`link` 分支改：

```ts
const name = ctx.resolveName(b)
lines.push('', `- ${b.title}：${name === null ? '失效引用' : (ctx.linkText ? ctx.linkText(name) : name)}`)
```

`characterToMarkdown` 签名加第 4 参并透传：

```ts
export function characterToMarkdown(
  c: Character, data: ProjectData, assets: AssetMeta[], opts: MdRenderOptions = {},
): MdExportResult {
  // ... ctx 构造处并入 assetUrl: opts.assetUrl, linkText: opts.linkText
```

- [ ] **Step 4: 跑全量导出测试确认 M6-F2 不回归**

Run: `npx vitest run tests/export.test.ts`
Expected: 全 PASS（含既有 M6-F2 用例）。

- [ ] **Step 5: Commit**（地基无独立版本号，并入 Task 4 的 3.2.0）

```bash
git add src/utils/mdExport.ts tests/export.test.ts
git commit -m "feat: mdExport 支持图片路径与链接文本渲染选项（F1 地基）"
```

---

### Task 2: workspace 纯函数——文件名工具 + 百科/事件转 md（F1 核心）

**Files:**
- Create: `src/utils/workspace.ts`
- Test: `tests/workspace.test.ts`

**Interfaces:**
- Consumes: `displayTime(time)` from `src/utils/branchOrder.ts`；类型 from `@/types`。
- Produces（Task 3/4 依赖，签名固定）：
  - `sanitizeFilename(name: string): string`
  - `dedupeNames(names: string[]): string[]`
  - `yamlFront(fields: Record<string, string | undefined>): string`（返回 `---\n...\n---\n` 或空串）
  - `codexToMarkdown(e: CodexEntry, typeName: string, frontmatter: boolean): string`
  - `eventToMarkdown(ev: TimelineEvent, data: ProjectData, frontmatter: boolean): string`

- [ ] **Step 1: 写失败测试**

新建 `tests/workspace.test.ts`（复用 `tests/export.test.ts` 的 `makeData` 形状，本地拷一份精简版）：

```ts
import { describe, it, expect } from 'vitest'
import { sanitizeFilename, dedupeNames, yamlFront, codexToMarkdown, eventToMarkdown } from '../src/utils/workspace'
import type { ProjectData } from '../src/types'
import { builtinTemplates } from '../src/data/builtinTemplates'

function makeData(): ProjectData {
  return {
    meta: { id: 'p1', name: '工作区测试', schemaVersion: 3, createdAt: '2026-01-01T00:00:00Z', updatedAt: '2026-01-01T00:00:00Z' },
    settings: {
      relationTypes: [],
      codexTypes: [{ id: 'ct1', key: 'location', name: '地点' }, { id: 'ct2', key: 'faction', name: '势力' }],
      worldlines: [{ id: 'w1', name: '主世界线', parentWorldlineId: null, forkPointEventId: null, color: '#7d9cb5', status: 'active', order: 0 }],
    },
    relations: [],
    templates: builtinTemplates(),
    characters: [{ id: 'c0', name: '角色0', createdAt: '2026-01-01T00:00:00Z', updatedAt: '2026-01-01T00:00:00Z', fieldBlocks: [] }],
    codex: [
      { id: 'x1', typeId: 'ct2', name: '王城', content: '参见 [[苍之海]]', attributes: [{ key: '人口', value: '3 万' }], color: '#8fae8b' },
      { id: 'x2', typeId: 'ct1', name: '苍之海', content: '毗邻 [[王城]]', attributes: [], color: '#7fb3ae' },
    ],
    events: [
      { id: 'e0', worldlineId: 'w1', time: { mode: 'calendar', era: '通用纪年', year: '217', month: '3', day: '' }, title: '建国', description: '描述正文', participantIds: ['c0'], relatedCodexIds: ['x1'], rank: 0, manualPlaced: false, collapsed: false, locked: false },
      { id: 'e1', worldlineId: null, time: null, title: '草稿事件', description: '', participantIds: [], relatedCodexIds: [], rank: 0, manualPlaced: false, collapsed: false, locked: false },
    ],
  }
}

describe('v2.4-F1 文件名工具', () => {
  it('非法字符替换 / 空名兜底', () => {
    expect(sanitizeFilename('a/b\\c:d*e?f"g<h>i|j')).toBe('a_b_c_d_e_f_g_h_i_j')
    expect(sanitizeFilename('  名字 ')).toBe('名字')
    expect(sanitizeFilename('名字...')).toBe('名字')
    expect(sanitizeFilename('')).toBe('未命名')
  })
  it('重名去重：首个不变，后续加 -2/-3', () => {
    expect(dedupeNames(['王城', '苍之海', '王城', '王城'])).toEqual(['王城', '苍之海', '王城-2', '王城-3'])
  })
})

describe('v2.4-F1 frontmatter', () => {
  it('含 # / : 的值自动加引号；空值跳过；空对象输出空串', () => {
    expect(yamlFront({ color: '#8fae8b', time: '通用纪年 217 年 3 月', rank: '0' })).toBe(
      '---\ncolor: "#8fae8b"\ntime: 通用纪年 217 年 3 月\nrank: 0\n---\n',
    )
    expect(yamlFront({ a: '', b: undefined })).toBe('')
  })
})

describe('v2.4-F1 百科 → md', () => {
  it('frontmatter 含类型名与颜色；属性成表；[[链接]] 原样保留', () => {
    const md = codexToMarkdown(makeData().codex[0], '势力', true)
    expect(md.startsWith('---\n')).toBe(true)
    expect(md).toContain('type: 势力')
    expect(md).toContain('color: "#8fae8b"')
    expect(md).toContain('# 王城')
    expect(md).toContain('| 人口 | 3 万 |')
    expect(md).toContain('参见 [[苍之海]]')
  })
  it('frontmatter=false 时无 --- 头', () => {
    expect(codexToMarkdown(makeData().codex[0], '势力', false)).not.toContain('---')
  })
})

describe('v2.4-F1 事件 → md', () => {
  it('时间字段 = displayTime；参与者/关联百科成 [[双链]]；草稿事件 worldline 标草稿', () => {
    const d = makeData()
    const md = eventToMarkdown(d.events[0], d, true)
    expect(md).toContain('time: 通用纪年 217 年 3 月')
    expect(md).toContain('# 建国')
    expect(md).toContain('[[角色0]]')
    expect(md).toContain('[[王城]]')
    expect(md).toContain('描述正文')
    const draft = eventToMarkdown(d.events[1], d, true)
    expect(draft).toContain('worldline: 草稿')
    expect(draft).not.toContain('time:')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/workspace.test.ts`
Expected: FAIL——模块不存在。

- [ ] **Step 3: 实现 `src/utils/workspace.ts`**

```ts
// v2.4-F1 Markdown 工作区导出：实体 → 可被 Obsidian/Typora/VS Code 打开的纯文本工作区。
// 不触碰 zip.ts 的项目格式（§3.1）；本文件只做「导出」，导回仍走 zip。
import type { CodexEntry, ProjectData, TimelineEvent } from '@/types'
import { displayTime } from '@/utils/branchOrder'
import { characterToMarkdown, type MdRenderOptions } from '@/utils/mdExport'
import { zipSync, strToU8 } from 'fflate'

/** Windows/Obsidian 双非法字符统一替换；空名兜底「未命名」 */
export function sanitizeFilename(name: string): string {
  const s = name.replace(/[\\/:*?"<>|#^]/g, '_').replace(/\s+/g, ' ').trim().replace(/[. ]+$/g, '')
  return s || '未命名'
}

/** 重名自动 -2/-3 后缀（百科名项目内已唯一，角色/事件名不保证） */
export function dedupeNames(names: string[]): string[] {
  const used = new Map<string, number>()
  return names.map((n) => {
    const c = used.get(n) ?? 0
    used.set(n, c + 1)
    return c === 0 ? n : `${n}-${c + 1}`
  })
}

/** 极简 YAML frontmatter：值含 # 或 : 时 JSON 引号包裹；空值字段跳过 */
export function yamlFront(fields: Record<string, string | undefined>): string {
  const lines = Object.entries(fields)
    .filter(([, v]) => v !== undefined && v !== '')
    .map(([k, v]) => `${k}: ${/[:#]/.test(v!) ? JSON.stringify(v) : v}`)
  return lines.length ? `---\n${lines.join('\n')}\n---\n` : ''
}

export function codexToMarkdown(e: CodexEntry, typeName: string, frontmatter: boolean): string {
  const parts: string[] = [frontmatter ? yamlFront({ type: typeName, color: e.color }) : '', `# ${e.name}`]
  if (e.attributes.length) {
    parts.push('', '| 属性 | 值 |', '| --- | --- |')
    for (const a of e.attributes) parts.push(`| ${a.key} | ${a.value} |`)
  }
  parts.push('', e.content)
  return parts.filter((s) => s !== '').join('\n') + '\n'
}

export function eventToMarkdown(ev: TimelineEvent, data: ProjectData, frontmatter: boolean): string {
  const wl = data.settings.worldlines.find((w) => w.id === ev.worldlineId)
  const parts: string[] = [frontmatter ? yamlFront({
    type: 'event',
    time: displayTime(ev.time) || undefined,
    worldline: wl?.name ?? '草稿',
    rank: String(ev.rank),
  }) : '', `# ${ev.title}`]
  const charName = (id: string) => data.characters.find((c) => c.id === id)?.name ?? null
  const codexName = (id: string) => data.codex.find((x) => x.id === id)?.name ?? null
  const ps = ev.participantIds.map((id) => charName(id) ?? codexName(id)).filter((n): n is string => n !== null)
  if (ps.length) parts.push('', `参与者：${ps.map((n) => `[[${n}]]`).join('、')}`)
  const cs = ev.relatedCodexIds.map(codexName).filter((n): n is string => n !== null)
  if (cs.length) parts.push('', `关联百科：${cs.map((n) => `[[${n}]]`).join('、')}`)
  if (ev.description) parts.push('', ev.description)
  return parts.filter((s) => s !== '').join('\n') + '\n'
}
```

注意 `codexToMarkdown` 里 `parts.filter(s => s !== '')` 会把空 content 行也滤掉——frontmatter 为空串时它必须被滤（否则开头多空行），正文里的空串（content 为空）滤掉无害。若测试暴露分组空行问题，改为只在头部单独拼接。

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run tests/workspace.test.ts`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/utils/workspace.ts tests/workspace.test.ts
git commit -m "feat: workspace 纯函数——文件名工具/frontmatter/百科与事件转 md（v2.4-F1）"
```

---

### Task 3: buildWorkspaceZip 整包组装 + 读回校验（F1 通过标准）

**Files:**
- Modify: `src/utils/workspace.ts`（追加）
- Test: `tests/workspace.test.ts`（追加）

**Interfaces:**
- Consumes: Task 2 全部函数；`characterToMarkdown(c, data, assets, opts)`（Task 1）；`ZipAsset`（`{ meta: Omit<AssetMeta,'projectId'>; bytes: Uint8Array }`）from `@/storage/zip`——仅类型导入，不改 zip.ts。
- Produces:
  - `buildWorkspaceZip(data: ProjectData, assets: ZipAsset[], opts: { frontmatter: boolean }): Uint8Array`
  - 布局（解压即工作区文件夹）：
    ```
    characters/<角色名>.md      图片引用 ../assets/<id>.<ext>，link 块 → [[名字]]
    codex/<条目名>.md           [[条目名]] 原样保留
    events/<事件标题>.md
    assets/<id>.<ext>           仅被图片块引用的资产（与 zip 导出 M7-F2 孤儿排除口径一致）
    ```

- [ ] **Step 1: 写失败测试**

`tests/workspace.test.ts` 追加：

```ts
import { buildWorkspaceZip } from '../src/utils/workspace'
import { unzipSync, strFromU8 } from 'fflate'

describe('v2.4-F1 buildWorkspaceZip 整包', () => {
  const d = makeData()
  d.characters[0].fieldBlocks = [
    { type: 'image', title: '立绘', assetId: 'a1' },
    { type: 'link', title: '认识', targetType: 'character', targetId: 'c1' },
  ]
  d.characters.push({ id: 'c1', name: '角色0', createdAt: '', updatedAt: '', fieldBlocks: [] }) // 重名角色
  const assets = [{ meta: { id: 'a1', ext: 'png', name: '立绘.png', mime: 'image/png', size: 4 }, bytes: new Uint8Array([1, 2, 3, 4]) }]

  function unzip(z: Uint8Array): Record<string, string> {
    const files = unzipSync(z)
    return Object.fromEntries(Object.entries(files).map(([p, u8]) => [p, strFromU8(u8)]))
  }

  it('逐实体断言：文件名去重 / 图片相对路径 / [[链接]] / frontmatter 时间字段', () => {
    const files = unzip(buildWorkspaceZip(d, assets, { frontmatter: true }))
    expect(Object.keys(files).sort()).toEqual([
      'assets/a1.png', 'characters/角色0-2.md', 'characters/角色0.md', 'codex/王城.md', 'codex/苍之海.md', 'events/建国.md', 'events/草稿事件.md',
    ].sort())
    expect(files['characters/角色0.md']).toContain('![立绘](../assets/a1.png)')
    expect(files['characters/角色0.md']).toContain('- 认识：[[角色0-2]]')
    expect(files['events/建国.md']).toContain('time: 通用纪年 217 年 3 月')
    expect(files['codex/王城.md']).toContain('参见 [[苍之海]]')
  })

  it('读回校验：每个实体的 md 标题与关键内容可逐项还原', () => {
    const files = unzip(buildWorkspaceZip(d, assets, { frontmatter: true }))
    for (const c of d.characters) {
      const names = [c.name, ...d.characters.filter((o) => o !== c && o.name === c.name)]
      void names
    }
    // 角色：标题 + 名字可从文件名还原
    const charFiles = Object.keys(files).filter((p) => p.startsWith('characters/'))
    for (const p of charFiles) {
      const c = d.characters.find((x) => p === `characters/${x.name}.md`) ?? d.characters.find((x) => p.startsWith(`characters/${x.name}`))
      expect(c).toBeTruthy()
      expect(files[p]).toContain(`# ${c!.name}`)
    }
    // 百科：标题 + 属性键值 + [[链接]] 全在
    expect(files['codex/王城.md']).toContain('# 王城')
    expect(files['codex/王城.md']).toContain('| 人口 | 3 万 |')
    // 事件：标题 + 参与者 + 描述
    expect(files['events/建国.md']).toContain('# 建国')
    expect(files['events/建国.md']).toContain('[[角色0]]')
    expect(files['events/建国.md']).toContain('描述正文')
  })

  it('frontmatter=false：无 --- 头；未引用资产不进包', () => {
    const files = unzip(buildWorkspaceZip(d, [...assets, { meta: { id: 'a9', ext: 'png', name: '孤儿.png', mime: 'image/png', size: 1 }, bytes: new Uint8Array([9]) }], { frontmatter: false }))
    expect(files['codex/王城.md'].startsWith('# 王城')).toBe(true)
    expect(Object.keys(files)).not.toContain('assets/a9.png')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/workspace.test.ts`
Expected: FAIL——`buildWorkspaceZip` 未导出。

- [ ] **Step 3: 实现**

`src/utils/workspace.ts` 追加：

```ts
import type { ZipAsset } from '@/storage/zip'
import type { AssetMeta, Character } from '@/types'

export interface WorkspaceOptions { frontmatter: boolean }

export function buildWorkspaceZip(data: ProjectData, assets: ZipAsset[], opts: WorkspaceOptions): Uint8Array {
  const entries: Record<string, Uint8Array> = {}
  const typeName = (typeId: string) => data.settings.codexTypes.find((t) => t.id === typeId)?.name ?? '未分类'

  const charNames = dedupeNames(data.characters.map((c) => sanitizeFilename(c.name)))
  const render: MdRenderOptions = {
    assetUrl: (a) => `../assets/${a.id}.${a.ext}`,
    linkText: (n) => `[[${n}]]`,
  }
  // ponytail: [[名字]] 按 Obsidian basename 解析；重名被去重为 名字-2 后，指向首个同名文件的链接会落到 -1 文件，重名角色罕见，接受
  const usedAssetIds = new Set<string>()
  data.characters.forEach((c: Character, i: number) => {
    const fm = opts.frontmatter ? yamlFront({ type: 'character' }) : ''
    const { md, usedAssets } = characterToMarkdown(c, data, assets.map((a) => ({ ...a.meta, projectId: data.meta.id })), render)
    for (const a of usedAssets) usedAssetIds.add(a.id)
    entries[`characters/${charNames[i]}.md`] = strToU8(fm + md)
  })

  const codexNames = dedupeNames(data.codex.map((e) => sanitizeFilename(e.name)))
  data.codex.forEach((e, i) => { entries[`codex/${codexNames[i]}.md`] = strToU8(codexToMarkdown(e, typeName(e.typeId), opts.frontmatter)) })

  const evNames = dedupeNames(data.events.map((e) => sanitizeFilename(e.title)))
  data.events.forEach((e, i) => { entries[`events/${evNames[i]}.md`] = strToU8(eventToMarkdown(e, data, opts.frontmatter)) })

  for (const a of assets) if (usedAssetIds.has(a.meta.id)) entries[`assets/${a.meta.id}.${a.meta.ext}`] = a.bytes
  return zipSync(entries, { level: 6 })
}
```

注意：`characterToMarkdown` 的 `usedAssets` 依赖传入的 assets 数组匹配 assetId——`ZipAsset.meta` 无 `projectId`，用展开补上（纯内存对象，不落库）。

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run tests/workspace.test.ts tests/export.test.ts`
Expected: 全 PASS（顺带确认 Task 1 无回归）。

- [ ] **Step 5: 门禁**

Run: `npm run check:tokens && npx vitest run`
Expected: 全绿（workspace.ts 无色值字面量——`type: character` 等均为普通字符串）。

- [ ] **Step 6: Commit**

```bash
git add src/utils/workspace.ts tests/workspace.test.ts
git commit -m "feat: buildWorkspaceZip——实体分文件夹成包+读回校验用例（v2.4-F1）"
```

---

### Task 4: 导出页工作区卡片（F1 UI）

**Files:**
- Modify: `src/views/ExportView.vue`
- Modify: `package.json`（3.1.6 → 3.2.0）
- Modify: `CHANGELOG.md`

**Interfaces:**
- Consumes: `buildWorkspaceZip(data, assets, opts)`（Task 3）；`store.assets` / `store.assetUrl(id)`（现有）；`downloadBlob`（现有）。
- Produces: 无（纯 UI）。

- [ ] **Step 1: 写卡片**

`ExportView.vue` 在「角色卡 Markdown」卡片之后插入（lucide 图标 `FolderOutput` 已在依赖图标集内）：

```vue
<section class="panel card">
  <div class="card-head">
    <h3><FolderOutput :size="16" /> Markdown 工作区导出</h3>
    <div class="ops">
      <n-checkbox v-model:checked="wsFrontmatter" size="small">frontmatter 元数据</n-checkbox>
      <n-button size="small" type="primary" :loading="busy.ws" @click="doExportWorkspace">导出工作区 zip</n-button>
    </div>
  </div>
  <p class="desc">
    每个角色 / 百科 / 事件一个 .md 文件（characters / codex / events 三文件夹），保留 [[条目名]] 双链与 assets 相对路径图片引用；解压后可直接用 Obsidian / Typora / VS Code 打开编辑。frontmatter 含类型 / 颜色 / 历法时间。
  </p>
</section>
```

script 追加（显式 import `NCheckbox`、`FolderOutput`）：

```ts
import { buildWorkspaceZip } from '@/utils/workspace'
const wsFrontmatter = ref(true)

async function doExportWorkspace(): Promise<void> {
  if (!store.current) return
  busy.ws = true
  try {
    const zs: { meta: AssetMeta; bytes: Uint8Array }[] = []
    for (const m of store.assets) {
      const url = await store.assetUrl(m.id)
      if (!url) continue
      const blob = await (await fetch(url)).blob()
      zs.push({ meta: m, bytes: new Uint8Array(await blob.arrayBuffer()) })
    }
    const zip = buildWorkspaceZip(store.current, zs, { frontmatter: wsFrontmatter.value })
    downloadBlob(new Blob([zip as BlobPart], { type: 'application/zip' }), `${store.current.meta.name}-工作区.zip`)
    message.success('Markdown 工作区已导出')
  } catch (e) {
    message.error(`导出失败：${e instanceof Error ? e.message : e}`)
  } finally { busy.ws = false }
}
```

`busy` reactive 加 `ws: false`；`AssetMeta` 类型加入现有 type import。

- [ ] **Step 2: 验证**

Run: `npm run build && npm run lint`
Expected: 0 error（8 条既有 warning 不变）。手动 `npm run dev` 导出一次确认文件名/链接正确（浏览器行为，测试已覆盖纯函数）。

- [ ] **Step 3: 版本与日志**

`package.json` → `"version": "3.2.0"`；`CHANGELOG.md` 顶部：

```markdown
## [3.2.0] - 2026-08-26

### 新增

- v2.4-F1 Markdown 工作区导出：每实体一个 .md（characters/codex/events 文件夹），保留 [[条目名]] 双链、assets 相对路径图片、可选 frontmatter（类型/颜色/历法时间）；产出可被 Obsidian/Typora/VS Code 打开；zip 项目格式（§3.1）不受影响。
```

- [ ] **Step 4: Commit**

```bash
git add src/views/ExportView.vue package.json CHANGELOG.md
git commit -m "feat: 导出页 Markdown 工作区导出卡片（v2.4-F1，3.2.0）"
```

---

### Task 5: backup 纯函数（F2 通过标准）

**Files:**
- Create: `src/utils/backup.ts`
- Test: `tests/backup.test.ts`

**Interfaces:**
- Produces（Task 6 依赖）：
  - `DEFAULT_BACKUP_DAYS = 7`
  - `backupDue(lastExportIso: string | null, nowIso: string, thresholdDays: number): boolean`
  - `loadStamp(projectId: string): string | null`、`saveStamp(projectId: string, iso?: string): void`
  - `loadThresholdDays(): number`、`saveThresholdDays(n: number): void`

- [ ] **Step 1: 写失败测试**

新建 `tests/backup.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { backupDue, DEFAULT_BACKUP_DAYS } from '../src/utils/backup'

const T0 = '2026-08-01T00:00:00Z'
const day = (n: number) => new Date(Date.parse(T0) + n * 86_400_000).toISOString()

describe('v2.4-F2 backupDue 边界', () => {
  it('从未备份 → 提醒', () => {
    expect(backupDue(null, T0, 7)).toBe(true)
  })
  it('距上次 N 天整（=阈值）→ 触发；少一秒 → 不触发', () => {
    expect(backupDue(T0, day(7), 7)).toBe(true)
    expect(backupDue(T0, day(6), 7)).toBe(false)
    expect(backupDue(T0, new Date(Date.parse(day(7)) - 1000).toISOString(), 7)).toBe(false)
  })
  it('阈值 0 / 负数 → 恒提醒（关闭无意义，视为总是）', () => {
    expect(backupDue(T0, day(1), 0)).toBe(true)
    expect(backupDue(T0, day(1), -1)).toBe(true)
  })
  it('戳损坏（非法 ISO）→ 提醒（宁误报不漏报）', () => {
    expect(backupDue('not-a-date', T0, 7)).toBe(true)
  })
  it('默认阈值 7 天', () => {
    expect(DEFAULT_BACKUP_DAYS).toBe(7)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/backup.test.ts`
Expected: FAIL——模块不存在。

- [ ] **Step 3: 实现 `src/utils/backup.ts`**

```ts
// v2.4-F2 备份提醒：纯函数判断 + localStorage 戳（UI 态，不入 dexie/zip——提醒不是项目数据）
const DAY_MS = 86_400_000
const STAMP_PREFIX = 'woc-backup-stamp:'
const THRESHOLD_KEY = 'woc-backup-threshold-days'

export const DEFAULT_BACKUP_DAYS = 7

export function backupDue(lastExportIso: string | null, nowIso: string, thresholdDays: number): boolean {
  if (!lastExportIso || thresholdDays <= 0) return true
  const elapsed = Date.parse(nowIso) - Date.parse(lastExportIso)
  if (!Number.isFinite(elapsed)) return true // 戳损坏，宁误报
  return elapsed >= thresholdDays * DAY_MS
}

export function loadStamp(projectId: string): string | null {
  try { return localStorage.getItem(STAMP_PREFIX + projectId) } catch { return null }
}
export function saveStamp(projectId: string, iso = new Date().toISOString()): void {
  try { localStorage.setItem(STAMP_PREFIX + projectId, iso) } catch { /* 隐私模式忽略 */ }
}
export function loadThresholdDays(): number {
  try {
    const n = Number(localStorage.getItem(THRESHOLD_KEY))
    return Number.isInteger(n) && n >= 1 && n <= 365 ? n : DEFAULT_BACKUP_DAYS
  } catch { return DEFAULT_BACKUP_DAYS }
}
export function saveThresholdDays(n: number): void {
  try { localStorage.setItem(THRESHOLD_KEY, String(Math.min(365, Math.max(1, Math.round(n))))) } catch { /* ignore */ }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run tests/backup.test.ts`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/utils/backup.ts tests/backup.test.ts
git commit -m "feat: backupDue 纯函数与 localStorage 戳（v2.4-F2）"
```

---

### Task 6: 备份提醒 UI——导出页横幅 + 侧栏圆点（F2 UI）

**Files:**
- Modify: `src/views/ExportView.vue`
- Modify: `src/App.vue`
- Modify: `package.json`（→ 3.3.0）
- Modify: `CHANGELOG.md`

**Interfaces:**
- Consumes: Task 5 全部函数；`store.exportZip()` 现有动作；`store.current?.meta.id`。

- [ ] **Step 1: 导出页——成功导出后打戳 + 到期横幅 + 阈值配置**

`ExportView.vue`：

```ts
import { backupDue, loadStamp, saveStamp, loadThresholdDays, saveThresholdDays } from '@/utils/backup'
const thresholdDays = ref(loadThresholdDays())
const stamp = ref<string | null>(store.current ? loadStamp(store.current.meta.id) : null)
const due = computed(() => stamp.value === null || backupDue(stamp.value, new Date().toISOString(), thresholdDays.value))
```

`doExportZip` 成功分支（`message.success` 前）追加：

```ts
if (store.current) { saveStamp(store.current.meta.id); stamp.value = new Date().toISOString() }
```

zip 卡片标题下插入横幅与阈值输入（`NAlert`、`NInputNumber` 显式 import）：

```vue
<n-alert v-if="due" type="warning" :bordered="false" style="margin-top: 10px">
  {{ stamp === null ? '本项目还没有导出过 zip 备份。' : `距上次 zip 备份已超过 ${thresholdDays} 天。` }}
  点击「导出 zip」完成备份后提醒自动消除。
</n-alert>
<div v-if="due" class="ops" style="margin-top: 8px">
  <span class="desc" style="margin: 0">提醒阈值（天）：</span>
  <n-input-number v-model:value="thresholdDays" size="small" :min="1" :max="365" style="width: 120px" @update:value="(v: number | null) => v && saveThresholdDays(v)" />
</div>
```

- [ ] **Step 2: 侧栏——导出导航项加圆点**

`src/App.vue` nav 段，`RouterLink` 内 name span 后追加（无新色值，用 `--accent` token；`useProjectStore` 已在文件作用域可引入）：

```vue
<span
  v-if="r.name === 'export' && backupDueNow"
  class="nav-dot"
  title="距上次 zip 备份已超阈值，去导出"
/>
```

script：

```ts
import { useProjectStore, reopenLastProject } from '@/stores/project'
import { backupDue, loadStamp, loadThresholdDays } from '@/utils/backup'
const projectStore = useProjectStore()
const backupDueNow = computed(() => {
  const id = projectStore.current?.meta.id
  if (!id) return false
  return backupDue(loadStamp(id), new Date().toISOString(), loadThresholdDays())
})
```

style（token 色，无字面量）：

```css
.nav-dot { width: 7px; height: 7px; border-radius: 50%; background: var(--accent); margin-left: auto; align-self: center; }
```

- [ ] **Step 3: 验证**

Run: `npm run build && npm run lint && npm run check:tokens`
Expected: 全过（无新 hex 字面量）。手动 dev：改 localStorage 戳为 8 天前 → 圆点+横幅出现；导出 zip → 消失。

- [ ] **Step 4: 版本与日志**（3.3.0，CHANGELOG 记「新增：v2.4-F2 备份提醒——距上次 zip 导出超 N 天（默认 7，可配）时侧栏圆点+导出页横幅提醒；不做自动化快照」）

- [ ] **Step 5: Commit**

```bash
git add src/views/ExportView.vue src/App.vue package.json CHANGELOG.md
git commit -m "feat: 备份提醒——导出页横幅+侧栏圆点+阈值配置（v2.4-F2，3.3.0）"
```

---

### Task 7: 角色卡 PDF 打印导出（F3，降优先级，可整体顺延）

> 若 v2.4 预算不足，本任务整体顺延至后续版本——前 6 个任务已独立可用。顺延时在 ROADMAP §2 v2.4-F3 标注顺延并记 CHANGELOG。

**Files:**
- Create: `src/utils/printExport.ts`
- Test: `tests/printExport.test.ts`
- Modify: `src/views/ExportView.vue`（角色卡卡片加「打印 / PDF」按钮，复用现有 `mdCharId` 选择器）
- Modify: `package.json`（→ 3.4.0）
- Modify: `CHANGELOG.md`

**Interfaces:**
- Consumes: `characterToMarkdown`（Task 1）、`marked`（已有依赖）、`readToken(name)` from `@/utils/tokens`。
- Produces:
  - `characterToPrintHtml(c: Character, data: ProjectData, tokens: Record<string, string>): string`（纯函数，node 可测；tokens 由浏览器端收集）
  - `collectPrintTokens(): Record<string, string>`（浏览器端：从 `document.documentElement` 取 `--bg/--surface/--text-1/--text-2/--text-3/--border/--accent-weak/--font-serif/--font-ui`）
  - `printHtml(html: string): void`（隐藏 iframe 调起打印对话框）

- [ ] **Step 1: 写失败测试**

新建 `tests/printExport.test.ts`（makeData 同 Task 2 精简版 + 全块型角色）：

```ts
import { describe, it, expect } from 'vitest'
import { characterToPrintHtml } from '../src/utils/printExport'
// makeData 同 tests/workspace.test.ts（可拷贝；含全块型角色 c0：kv/text/list/image/table/link/group）

const TOKENS = { '--bg': '#fff', '--surface': '#fafafa', '--text-1': '#222', '--text-2': '#555', '--text-3': '#888', '--border': '#ddd', '--accent-weak': '#eee', '--font-serif': 'serif', '--font-ui': 'sans-serif' }

describe('v2.4-F3 角色卡打印 HTML', () => {
  it('包含全部块标题与内容（无截断）+ 打印分页规则 + 双主题 token 内联', () => {
    const d = makeData()
    const html = characterToPrintHtml(d.characters[0], d, TOKENS)
    expect(html).toContain('<!DOCTYPE html>')
    for (const t of ['基本信息', '背景', '标签', '立绘', '属性', '挚友', '深层']) expect(html).toContain(t)
    expect(html).toContain('**幼年**'.replace(/\*\*/g, '')) // marked 渲染后 <strong>幼年</strong> 或纯文本，标题级断言即可
    expect(html).toMatch(/break-inside:\s*avoid/)
    expect(html).toContain('@page')
    for (const v of Object.values(TOKENS)) expect(html).toContain(v)
  })
})
```

- [ ] **Step 2: 跑测试确认失败** → `npx vitest run tests/printExport.test.ts` FAIL（模块不存在）。

- [ ] **Step 3: 实现 `src/utils/printExport.ts`**

```ts
// v2.4-F3：角色卡 Markdown → HTML → 打印（浏览器「另存为 PDF」）。双主题靠运行时收集 token 内联，不引入新依赖。
import { marked } from 'marked'
import type { AssetMeta, Character, ProjectData } from '@/types'
import { characterToMarkdown } from '@/utils/mdExport'

const TOKEN_NAMES = ['--bg', '--surface', '--text-1', '--text-2', '--text-3', '--border', '--accent-weak', '--font-serif', '--font-ui'] as const

export function collectPrintTokens(): Record<string, string> {
  const cs = getComputedStyle(document.documentElement)
  return Object.fromEntries(TOKEN_NAMES.map((n) => [n, cs.getPropertyValue(n).trim()]))
}

export function characterToPrintHtml(c: Character, data: ProjectData, tokens: Record<string, string>): string {
  const { md } = characterToMarkdown(c, data, data ? [] : [])
  const body = marked.parse(md) as string
  const vars = Object.entries(tokens).map(([k, v]) => `${k}:${v}`).join(';')
  return `<!DOCTYPE html><html><head><meta charset="utf-8"><title>${c.name}</title><style>
:root{${vars}}
@page{size:A4;margin:16mm}
body{background:var(--bg);color:var(--text-1);font-family:var(--font-ui);margin:0;padding:24px;line-height:1.8}
h1{font-family:var(--font-serif)}
img{max-width:100%;max-height:180mm;object-fit:contain}
table{border-collapse:collapse;width:100%}th,td{border:1px solid var(--border);padding:6px 10px}
h2,h3{border-bottom:1px solid var(--border);padding-bottom:4px}
section{break-inside:avoid}
</style></head><body>${body}</body></html>`
}

export function printHtml(html: string): void {
  const iframe = document.createElement('iframe')
  iframe.style.cssText = 'position:fixed;right:0;bottom:0;width:0;height:0;border:0;opacity:0'
  iframe.srcdoc = html
  iframe.onload = () => {
    iframe.contentWindow?.focus()
    iframe.contentWindow?.print()
    setTimeout(() => iframe.remove(), 60_000) // ponytail: 简单延时清理；打印对话框阻塞时间不定，60s 兜底
  }
  document.body.appendChild(iframe)
}
```

（实现时按 marked 输出把 `h2` 段包 `<section>`：直接对 body 做 `body.replace(/<h[23][^>]*>/g, m => `<section>${m}`)` + 尾部闭合过于脆弱——更稳做法：marked 渲染前把 md 按 `## ` 切块分别渲染再各自包 section。以测试断言 break-inside 出现为准，选实现量小的。）

- [ ] **Step 4: 跑测试确认通过** → `npx vitest run tests/printExport.test.ts` PASS。

- [ ] **Step 5: ExportView 接线**

「角色卡 Markdown」卡片 ops 区加按钮（未选角色时禁用）：

```vue
<n-button size="small" :disabled="!mdCharId" :loading="busy.pdf" @click="doPrintPdf">打印 / PDF</n-button>
```

```ts
import { collectPrintTokens, characterToPrintHtml, printHtml } from '@/utils/printExport'
async function doPrintPdf(): Promise<void> {
  const c = store.current?.characters.find((x) => x.id === mdCharId.value)
  if (!c || !store.current) return
  busy.pdf = true
  try { printHtml(characterToPrintHtml(c, store.current, collectPrintTokens())) }
  finally { busy.pdf = false }
}
```

`busy` 加 `pdf: false`。手动验证：打印对话框出现、当前主题配色正确、长角色（>1 页）分页无截断（AC「无截断」为人工确认项，浏览器打印无法自动化断言）。

- [ ] **Step 6: 门禁 + 版本 + Commit**（3.4.0；`npm run build && npm run lint && npm run check:tokens && npx vitest run` 全绿后提交 `feat: 角色卡 PDF 打印导出（v2.4-F3，3.4.0）`）

---

### Task 8: v2.4 验收归档与 ROADMAP 收口

**Files:**
- Modify: `docs/acceptance/RELEASE.md`（附录 B 追加 v2.4 条目）
- Modify: `ROADMAP.md`（§2 v2.4 标注完成；F3 若顺延则标注）

- [ ] **Step 1: 全量门禁**

Run: `npm run test && npm run coverage && npm run check:tokens && npm run lint && npm run build`
Expected: 全绿（coverage 门槛 storage/utils/schemas ≥70% 不回退；workspace.ts / backup.ts / printExport.ts 已有测试计入）。

- [ ] **Step 2: RELEASE.md 附录 B 归档**

追加 v2.4 条目（沿用附录 B 既有格式）：F1 逐实体 md 断言 / 读回校验 / zip 布局不受影响（`tests/workspace.test.ts` 输出摘要）；F2 `backupDue` 边界断言（`tests/backup.test.ts`）；F3 打印 HTML 生成断言 + 双主题人工确认记录；运行 `npx vitest run` 的通过数截图或文本。

- [ ] **Step 3: ROADMAP.md 收口**

§2 表格 v2.4 行标注「已完成（3.2.0–3.4.0，2026-08-XX）」；若 F3 顺延，在 v2.4-F3 小节标「→ 顺延至 vX.Y」并同步 §1 维护约定记录到 CHANGELOG。

- [ ] **Step 4: Commit**

```bash
git add docs/acceptance/RELEASE.md ROADMAP.md
git commit -m "docs: v2.4 验收证据归档与路线图收口"
```

---

## Self-Review 结论

- **Spec 覆盖**：F1 三条通过标准 → Task 2/3（逐实体断言、读回校验、zip 布局不动 = Global Constraint + zip.ts 零改动）；F2 → Task 5（边界纯函数单测）+ Task 6（提醒+一键导出=既有按钮，无自动化快照）；F3 → Task 7（可顺延已标注）。Obsidian 可用 = `[[条目名]]` 原样 + 文件名=条目名 + 相对路径 assets，均有断言。
- **已知取舍**（ponytail 注释已标）：重名角色去重后 `[[名字]]` 双链指向首个同名文件；`relatedCodexIds` 解析失败的引用静默省略（与既有失效引用展示策略不同但更符合纯文本工具预期）。
- **类型一致性**：`MdRenderOptions`（Task 1 定义，Task 3 消费）、`ZipAsset`（zip.ts 既有导出，Task 3 仅类型引用）、`WorkspaceOptions`（Task 3 定义，Task 4 消费）、backup 五函数（Task 5 定义，Task 6 消费）——签名已逐一核对。
