# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)：功能新增递增次版本号、修复递增修订号、破坏性变更（含数据格式变更）递增主版本号。
**每次修改后在本文件顶部追加条目**（维护约定见 AGENTS.md）。

## [2.3.0] - 2026-08-20

### 新增
- 时间轴背景**序位网格竖线**：整数序位处的淡竖线随缩放/平移/惯性一起移动，间距变化暗示当前缩放比例（缩远时按 1/5/25… 自适应抽稀，保持 ≥14px 间距不过密）
- 图谱背景**点阵**：26px 间距的淡点阵（token 取色随主题切换），节点平移/缩放时相对静态点阵运动，暗示画布可移动

## [2.2.0] - 2026-08-20

### 新增
- 时间轴拖拽**惯性滑动**：松手后按释放速度继续滑行一小段（指数衰减 τ=180ms，速度上限 2.5px/ms，滑行约 ≤450px），提升浏览手感
- 速度估算取最近 120ms 轨迹样本（慢速起步+快速甩动只按近段速度）；滚轮缩放/再次按下/离开页面即打断滑行
- `prefers-reduced-motion` 用户自动跳过惯性；掉帧保护（单帧 dt 封顶 48ms）避免瞬移

## [2.1.0] - 2026-08-20

### 新增
- 全应用动效（遵循设计文档 §5.7：150–250ms ease-out、克制不干扰创作、`prefers-reduced-motion` 时全部关闭）：
  - 页面切换入场（淡入上浮 8px；main 按路由重建仅做入场动画，规避免量多根片段的离场问题）
  - 首页项目卡片错落入场（40ms 递进，封顶 200ms）
  - 列表项浮现动画（角色/百科条目/事件列表/类型分组，搜索过滤时轻微浮现）
  - 时间轴：聚簇展开事件点弹出动画（以自身中心缩放）、事件圆点悬停放大（CSS `r` 过渡）
  - 按压反馈（侧栏功能键/小操作按钮/主题切换按压缩放）、主题切换图标悬停旋转
  - 面板随主题切换平滑过渡背景/边框/阴影

## [2.0.0] - 2026-08-20

### 变更（数据格式 v2，自动迁移）
- **关系箭头三态**：`RelationType.directed` 布尔升级为 `arrow: 'none' | 'single' | 'double'`——两个人之间支持**无箭头 / 单箭头 → / 双箭头 ↔**三种关系方向表达
- 渲染：单箭头画终点箭头，双箭头首尾各一，无箭头纯线条；图例与类型下拉同步显示箭头符号；类型管理中"有向"勾选框改为三态选择器
- **迁移双通道**：zip 导入旧版本数据自动升级（v1 directed → single/none，v0 内联 → relationTypes）；**浏览器内存量旧数据在打开项目时也自动升级并回写**（此前仅导入路径迁移）
- schemaVersion 1 → 2（旧版客户端无法读取 v2 数据，故升主版本号；升级本身无缝）

## [1.5.1] - 2026-08-20

### 修复
- 关系图谱默认缩放过大的问题：自适应视图增加 120px 内边距并将缩放钳制在 [0.15, 1.25]——少量角色节点不再被放大到铺满画布（保持约 38px 原始尺寸），大图谱仍可充分缩小查看全貌

## [1.5.0] - 2026-08-20

### 变更
- **时间轴 x 轴改为事件序位轴**：全局（跨世界线）按时间先后等距排布（同刻共享序位），不再按时间偏移量比例定位——悬殊的时间间隔（如 5 年与 1000 年）不再造成事件挤压或稀疏
- 移除时间刻度线（序位轴无时间刻度意义），改为在每个事件点/聚合点下方直接标注历法时间文本
- 轨道空白处点击插入：新事件时间取点击位置两侧邻事件的中值（保持先后顺序落在其间）
- 缩放/平移作用于序位空间：最深放大 0.05 序位、最远缩小为序位全景外扩两格（随事件数自适应）

## [1.4.1] - 2026-08-20

### 修复
- 时间轴缩放加尺度边界：最大放大限制在 0.01 绝对纪元单位跨度（月历下分钟级刻度仍可辨），最小缩小限制在百万年跨度；越界滚轮缩放以视域中点为界钳制，不再出现无限放大刻度退化或无限缩小事件消失

## [1.4.0] - 2026-08-20

### 变更
- **同刻多事件改为折叠聚簇**：轨道上同一时间点的多个事件默认收拢为一个带计数（×N）的聚合点，悬停显示事件清单，**点击展开**为纵向错开的独立事件（可分别点选），展开后提供「收起 ×N」入口；侧栏「全部事件」列表始终可直接选中
- 轨道整体下移（首轨基线 46→92px），展开态负向错开的事件不再被顶部工具栏遮挡

## [1.3.0] - 2026-08-20

### 新增
- 时间轴侧栏新增**「全部事件」列表**：按世界线分组（轨道色标识 + 计数）、组内按绝对纪元升序，点击任意事件直接打开编辑抽屉——彻底解决事件重叠无法选中问题

### 修复
- 同刻（相同绝对纪元）事件在轨道上纵向扇形错开（0/±16/±32px 循环），圆点与标签均可独立点选

## [1.2.0] - 2026-08-20

### 变更
- 角色页交互重构：**新建角色后直接进入编辑态**（无需再点「编辑」二级入口）；编辑过程**自动保存**（输入停顿 800ms 落库，提示节流防刷屏），「完成」退出并保存；编辑中切换角色自动落盘当前草稿，不再弹"放弃修改"确认框

### 修复
- 模板选择弹窗"跳过，直接创建空白角色"此前只关闭弹窗而未创建角色，现正确创建空白角色并直接进入编辑

## [1.1.0] - 2026-08-20

### 变更
- 侧栏导航改为显式排序（路由 `meta.order`），「导出」移至末位

### 新增
- `favicon.svg`（石纹标识，冷灰主题色），消除无图标时的 404 隐患
- README 完整部署流程（Nginx 配置与上线步骤 / GitHub Pages / 其他静态托管 / 升级回滚 / V2 预留）
- 应用内版本号显示（侧栏底部，读取 package.json）
- 本更新日志与版本号维护约定

## [1.0.0] - 2026-08-20

首个完整版本。M0–M7 全部里程碑与 G1–G13 全局验收标准通过（74 单元测试 + 33 E2E × Chromium/Firefox/Edge），验收记录见 [docs/acceptance/RELEASE.md](./docs/acceptance/RELEASE.md)。

### 核心功能
- **角色表**：七种自由字段块（分组/键值/Markdown/列表/图片/表格/链接）+ 模板系统（保存/加载/插入/跨项目分享）
- **世界观百科**：六内置类型 + 自定义类型，`[[条目名]]` 双向链接（名称全局唯一）、反向引用、属性模板
- **时间线与世界线**：类 Git 分叉模型（任意事件 fork 出 IF 线）、fork 点继承语义与失效容错、时间轴（轨道/分叉曲线/缩放平移）与画布（自由拖拽/因果连线）双视图同源、自定义历法线性换算
- **关系图谱**：G6 力导向布局、自定义关系类型（颜色/方向）、界面内建边/编辑、类型过滤、节点跳转角色卡
- **导出与分享**：zip 完整备份（与内部存储同构、排除孤儿资产）、角色卡 Markdown、图谱/时间轴/角色卡 PNG、断网可用的单文件 HTML 分享快照（双主题）
- **完整性巡检**：失效引用扫描分类、孤儿资产检测清理
- **工程能力**：冷灰大理石双主题（程序化纹理、WCAG AA、12 色低饱和数据调色板）、脏实体防抖存储、zod strict 校验 + schemaVersion 迁移管道、全局快捷键、三浏览器 E2E 矩阵
